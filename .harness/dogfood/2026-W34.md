# Dogfood 2026-W34 — 2026-08-18 / 2026-08-19

> 当前一行：`2026-W34-R3 Chat:pass Memory:fail Work:pass Desktop:pass Inbox:pass`
> 对照 `docs/05-engineering/development.md` 自用检查表。真实 LLM 走查，Windows 本机（PowerShell 5.1，仓库根 `.venv`）。

## R3（2026-08-19，复用已运行栈）

环境：复用本机已跑栈 —— 后端 `0.0.0.0:8000`（uvicorn `--reload`，DeepSeek）+ Vite `:5173`（HTTP 200）+ 本轮 `npm start` Electron（日志「Backend already running on port 8000」+「WebSocket connected for notifications」）。健康检查 `startup.status=ok`（MCP 6 总 / 3 连接 / 0 失败）。暗号 `HUASHAN-DF-W34-R3-0819`。用于验证 R2 之后的去重/收件箱/简报修复。

soak 终态：messages 244、memories 81（ratified 45 / proposed 1）、work_items completed 7、inbox_emails 22、approvals approved 391、notifications 11。

| 闭环 | 标记 | 证据 |
|------|------|------|
| Chat | **pass** | 会话 `b604db53-…`：记住暗号 → `write_file` 弹出 `confirmation_required`（`apr_2e0c1537…`）→ `decision=approve` → `.agent-work\dogfood_w34_r3.txt` 落盘 `HUASHAN-DF-W34-R3-0819`（22 字节，已核实磁盘内容） |
| Memory | **fail** | 抽取**已落库** proposed `2f997c12-…`（"…2026-W34-R3 is HUASHAN-DF-W34-R3-0819"）并 `POST /ratify` → `ratified`。`GET /memories/search?q=HUASHAN-…` 首条即该记忆（R2 近义去重吞更新已不再复现）。但新会话 `08812dc7-…` 问「W34-R3 暗号」只答出已批准的 `TIANSHAN-DF-W34-0818`。随后抽取又写入一条错误 proposed：`2026-W34-R3 dogfood 暗号为 TIANSHAN-DF-W34-0818`（`ad3c5669-…`，未批准）。判断：检索对精确标识符可用，Chat 召回仍偏向旧 standing decision。 |
| Work | **pass** | 目标 `4a00b84b-…` → decompose 6 步预览 → 子任务 `551e3a7d-…` pending→running→completed → 目标 completed progress=1.0，并产生 `goal_complete` 通知。完成步骤**没有**再写入「完成了行动步骤」proposed（R2 卡点 2 已收口） |
| Desktop | **pass** | Electron 日志确认复用 :8000 + WS 已连。今日 08:00 `TimerFired` 曾因固定 title 折叠（已修并补发 `早安简报 - 2026-08-19`）。本轮 `POST /morning-brief/test?persist=true` 生成文案「收件箱: 22 封（未读 0）」，同日 persist 按 `dedup_key` 折叠未覆盖旧正文。本侧无法截屏确认 Windows 通知横幅。 |
| Inbox | **pass** | `POST /settings/email/test` imap_ok/smtp_ok；`POST /inbox/poll` ok（本轮 new_count=0、duplicate=1；7 日累计 poll 200 次）；`GET /inbox/` **默认即 22 封**（R2 卡点 3 已收口）；`?status=pending` 仍为 0 |

### R3 卡点（下周输入）

1. **Chat 召回偏向旧暗号**：HUASHAN 已 ratified 且精确搜索排第一，但新会话问 W34-R3 仍答 TIANSHAN；错误回答随后被抽成一条错误 proposed。R2 是「抽取被近义去重吞掉」；本轮抽取已通，卡在召回/回答选择。
2. **同日二次 persist 不刷新简报正文**：`dedup_key=morning_brief:{date}` 故意折叠，诊断接口 `persist=true` 无法用新文案覆盖已读的当日简报。
3. **08:00 定时晨报依赖进程在跑**：本轮未做冷启动后的次日 08:00 观察（今日补发已验证分桶）。

### R3 明确没测

- 冷启动后的次日 08:00 Morning Brief
- Windows 通知横幅截屏、托盘交互
- SMTP 发信、Chat 崩溃续跑
- `deadline_alert` 真 09:00 触发（代码已分桶，本轮无即将到期目标）

---

## R2（同日第二轮，冷启动）

环境：杀进程后冷启动 —— 后端 `127.0.0.1:8000`（uvicorn，DeepSeek `deepseek-v4-flash`）+ Vite `127.0.0.1:5173`（HTTP 200）+ Electron（`npm start`，复用 :8000）。健康检查 `startup.status=ok`（MCP 6 总 / 3 连接 / 0 失败）。暗号 `HENGSHAN-DF-W34-R2-0818`。`MEMORY_EXTRACTOR=cloud`（Ollama 本轮未监听 11434）。

soak 终态：messages 228、memories 76（ratified 39 / proposed 2）、work_items completed 5、inbox_emails 21、approvals approved 315、notifications 7。

| 闭环 | 标记 | 证据 |
|------|------|------|
| Chat | **pass** | 会话 `0a227476-…`：记住暗号 → `write_file` 弹出 `confirmation_required`（`apr_8610ec33…`）→ `decision=approve` → `.agent-work\dogfood_w34_r2.txt` 落盘 `HENGSHAN-DF-W34-R2-0818`（23 字节，已核实磁盘内容） |
| Memory | **fail** | 抽取 LLM 调用成功（`llm_4708`，purpose=`memory_extract`，28 completion tokens），但 **没有新的 MemoryDerived**。180s 轮询 proposed 无 HENGSHAN。新会话 `7021d4b4-…` 只答出已批准的 `TIANSHAN-DF-W34-0818`。`GET /memories/search?q=HENGSHAN-…` 首条仍是 TIANSHAN。判断：新暗号被语义去重（阈值 cosine 0.85）当成 TIANSHAN 近义句丢掉。本轮未 ratify 任何已有记忆。 |
| Work | **pass** | 目标 `8c76a38d-…` → decompose 6 步预览 → 子任务 `b7abaab4-…` pending→running→completed → 目标 completed progress=1.0，并产生 goal_complete 通知 |
| Desktop | **pass** | Electron 日志「Backend already running on port 8000」+「WebSocket connected for notifications」。`POST /morning-brief/test?persist=true` 持久化成功。本侧无法截屏确认 Windows 通知横幅（与 R1/W33 同口径）。 |
| Inbox | **pass** | `POST /settings/email/test` imap_ok/smtp_ok；`POST /inbox/poll` ok（本轮 new_count=0、duplicate=1；7 日累计 poll 194 次 / new 61）；`GET /inbox/?status=all` 可见 21 封真实 Gmail；默认 `status=pending` 仍返回 `[]` |

### R2 卡点（下周输入）

1. **Memory 近义去重吞掉「更新后的 standing decision」**：已批准的 W34 暗号与新的 W34-R2 暗号语义过近，抽取成功但不落库，后续 Chat 继续答旧暗号。连续两轮 dogfood 才能暴露；R1 单次通过看不出来。
2. **完成 work item 会写入 proposed**：「完成了行动步骤：…」走 action-complete 旁路，不经过 extractor 噪声过滤。R1 留下 1 条，R2 又加 1 条；简报 `proposed_count=2`。
3. **`GET /api/inbox/` 默认 `status=pending`**：R1 卡点仍在，默认列表恒空。
4. **早安简报「未读邮件: 0」与 inbox 口径不一致**：库里 21 封，简报仍报 0。

### R2 明确没测

- 08:00 定时 Morning Brief（仍是手动 `persist=true`）
- Windows 通知横幅截屏、托盘交互
- SMTP 发信、Chat 崩溃续跑

---

## R1（系统冷启动 + 全闭环）

环境：系统冷启动 —— 后端 `127.0.0.1:8000`（uvicorn，DeepSeek `deepseek-v4-flash`）+ Vite `:5173`（HTTP 200）+ Electron（`npm start`，复用 :8000）。健康检查 `startup.status=ok`（MCP 6 总 / 3 连接 / 0 失败）。暗号 `TIANSHAN-DF-W34-0818`。

soak 终态：messages 222、memories 75（ratified 39 / proposed 1）、work_items completed 3、inbox_emails 21、approvals approved 311、notifications 6。

| 闭环 | 标记 | 证据 |
|------|------|------|
| Chat | **pass** | 会话 `c049c451-…`：记住暗号 → `write_file` 弹出 `confirmation_required`（`apr_9c488913…`）→ `decision=approve` → `.agent-work\dogfood_w34.txt` 落盘 `TIANSHAN-DF-W34-0818`（20 字节，已核实磁盘内容） |
| Memory | **pass** | 抽取出 proposed claim `069511f2-…`（"The user's dogfood code for 2026-W34 is TIANSHAN-DF-W34-0818"）→ `POST /ratify` 返回 `ratified` → 新会话 `70d48430-…` 精确答出暗号 |
| Work | **pass** | 目标 `39cd764f-…` → decompose 5 步预览 → 子任务 `c510dd69-…` pending→running→completed → 目标 completed progress=1.0，并产生 goal_complete 通知 |
| Desktop | **pass** | Electron 首启自动下载二进制后运行，日志确认「Backend already running on port 8000」+「WebSocket connected for notifications」。`POST /morning-brief/test?persist=true` 两次持久化成功。本侧无法截屏确认 Windows 通知横幅（与 W33 同口径）。 |
| Inbox | **pass** | `POST /settings/email/test` imap_ok/smtp_ok；`POST /inbox/poll` ok（本轮 new_count=0、duplicate=1；7 日累计 poll 191 次 / new 61）；`GET /inbox/?status=all` 可见 21 封真实 Gmail 邮件；inbox_digest 通知含重要邮件分类 |

### 本轮卡点（下周输入）

1. **`GET /api/inbox/` 默认 `status=pending`**：poll 过的邮件都已是 read，默认列表恒为空，日用观感像「收件箱没东西」。前端如果也用默认参数会踩同一坑。
2. **早安简报「未读邮件: 0」与 inbox 口径不一致**：W33 卡点 2 未变，库里 21 封邮件简报仍报 0。
3. **proposed 不 ratify 不进 Chat**：设计如此（W33 已确认），但确认入口远的问题仍在；本轮 proposed 噪声已明显减少（终态仅 1 条待确认）。
4. Electron 首次 `npm start` 要现场下载 Electron 二进制（约 1 分钟），desktop `npm ci` 后并未预装。

### 本轮明确没测

- 08:00 定时 Morning Brief（仍是手动 `persist=true`）
- Windows 通知横幅截屏、托盘交互
- SMTP 发信、Chat 崩溃续跑
