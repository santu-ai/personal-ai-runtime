# Dogfood 2026-W34 — 2026-08-17

> 当前一行：`2026-W34 Chat:pass Memory:fail Work:pass Desktop:untested Inbox:pass`
> 对照 `docs/05-engineering/development.md` 自用检查表。Windows 本机会话；日用库 `<repo>/data/personal_ai.db`。

## 库快照（soak 自动解析 SQLITE_PATH）

event_log 有增长（本轮 Work + Chat）、conversations 本轮新建 2 条 dogfood 会话、memories 2（全是 proposed，0 ratified）、work_items 本轮创建 1 条并完成、inbox_emails 0、llm_calls 已有真 LLM 回合。CapabilityFailed 0。

## 闭环

| 闭环 | 标记 | 证据 |
|------|------|------|
| Chat | **pass** | 运行中 API：创建会话 `W34 dogfood chat EN`，SSE `text_delta`+`done`，助手回了一句自我介绍。第二轮 `write_file` 弹出 `confirmation_required`（`apr_89740285595c4c0f82ff54a82cd609f1`），`POST …/resolve {deny}` 返回 `denied`，文件未写入。 |
| Memory | **fail** | 库内 2 条 claim 仍是 `proposed`。未替用户自动确认。Chat / 首页已能就地确认/拒绝（不再只能跳 Memories）。 |
| Work | **pass** | `POST /api/work-items` 创建 `W34 dogfood: pending-to-completed shortcut` → `POST …/status {completed}` **200**，`completed_at` 与 `progress=1.0` 已写上。这是本周修的 FSM 捷径。 |
| Desktop | **untested** | 本会话未起 Electron / Vite。 |
| Inbox | **pass** | health `email.configured=true`；`POST /inbox/poll` `status=ok new_count=0`。空箱是真实 IMAP 结果，不是 blocked。 |

## 本周从库/日用抓到并已修

1. soak 默认路径找不到本机库（实际在 `<repo>/data/`）。
2. Goals 勾选走 `WorkItemUpdated`，父 goal 进度只在 `WorkItemStatusChanged` 重算 → 进度停住。
3. pending→completed 被 FSM 判非法，但 UI 勾选就是这条路径。
4. email 空箱 / 缺 Message-ID 仍返回 `{"error":…}` JSON，Kernel 记成 CapabilityInvoked。
5. proposed 记忆确认入口只在会话页外链到 Memories；首页完全没有。现为 Chat/Home 内联确认条。
6. `ChatRequested` Lane A `max_retries` 1→2（第三次崩溃仍 DLQ）。需重启后端进程才生效。

## 未做

- Desktop 托盘通知
- SMTP 发信
- Context live A/B / Chroma 质量
- ADR-R011 审批续写仍 one-shot（deny 现在会写入会话说明，仍不调 LLM）
