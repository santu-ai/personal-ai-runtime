# Dogfood 2026-W35 — 2026-08-31

> 当前一行：`2026-W35 Chat:pass Memory:pass Work:n/a Desktop:n/a Inbox:pass 今天工作台:pass`
> 对照 `docs/05-engineering/development.md` 自用检查表。真实 LLM（`deepseek-v4-flash`）走查，Windows 本机（PowerShell 5.1，仓库根 `.venv`）。
> 本轮范围：Memory 两轮 + 「今天」30 秒 SLA + Chat 审批后续写（同一工具环）。Telegram / proposed 过期策略不纳入。

## 环境前提

- 健康检查 `GET /api/system/health`：`startup.status=ok`，`llm.configured=true`，`email.configured=true`。
- `.env` 已有有效 `LLM_API_KEY`；`MEMORY_EXTRACTOR=cloud`；`ALLOW_CLOUD_PERSONAL_DATA_EGRESS=true`。未修改、未提交 `.env`。
- 外部 MCP 本轮关闭（`MCP_EXTERNAL_ENABLED=false`），避免 stdio mesh 卡住启动。
- **日用库不在本机**：soak 无 `--db` 时落到仓库 `data/personal_ai.db`（240 KB，几乎空，仅 1 条 `EgressAudited`），不是 macOS 日用库。本轮真实验收使用新建的 `backend/data/personal_ai.db`。

## 自动化基线

| 检查 | 结果 |
|------|------|
| `scripts/soak_dogfood_report.py`（默认） | 空库：messages 0 / memories 0 / inbox 0 |
| soak `--db backend/data/personal_ai.db`（dogfood 后） | event_log 271；messages 12；memories 3；approvals approved 3；inbox_emails 20；MemoryDerived 3 |
| `python -m scripts.verify_memory_lifecycle` | PASSED |
| `python -m scripts.verify_vector_consistency` | self-test PASSED |
| 前端 `trust-loops` / 审批 e2e | 见本轮验证提交；三栏去重由既有 spec 覆盖 |

## Memory 两轮

暗号：`TAISHAN-DF-W35-0831` → `HUANGSHAN-DF-W35-0831-R2`。Driver：`.agent-work/dogfood_w35_driver.py`（不入库）。

| 轮次 | 标记 | 证据 |
|------|------|------|
| R1 | **pass** | 会话 `774be1bc-…`：表达事实 + `write_file` 弹出 `apr_366c763d…` → approve 后续写 `assistant_message` 非空 → proposed `e4d6034c-…` → ratify → 新会话召回仅 `TAISHAN-DF-W35-0831`。ratify 后 `GET /memories/search?q=TAISHAN-…` 首条即该记忆。磁盘 `.agent-work/dogfood_w35.txt` 19 字节。公开 search 在 ratify 前为空（claim 过滤，属既有行为）。 |
| R2 | **pass** | 会话 `7578306a-…`：新暗号 → `apr_65eba3ea…` 批准后续写 → proposed `5453d7a2-…` → ratify。新会话召回 `HUANGSHAN-DF-W35-0831-R2` 且不含 TAISHAN。搜索首条为 HUANGSHAN。旧记忆 `e4d6034c-…` `claim_status=rejected` / `reject_reason=superseded`。 |

终态 memories：1 ratified（HUANGSHAN）+ 1 rejected（TAISHAN superseded）+ 1 条中文复述 proposed（`8e5f80fa-…`，抽取旁路，未计入召回失败）。

## Chat 审批续写（真 LLM）

两轮 `write_file` 批准后 HTTP resolve 均返回 `status=success` 且带完整 `assistant_message`（不再是空的 one-shot）。本轮模型未在同一回复里再要第二个敏感工具；连续二次审批由单测 / Playwright 覆盖。

## 今天工作台 30 秒

打开 `http://127.0.0.1:5173/dashboard`（跳过 onboarding 后）：

- 三栏可见：「需要你决定」「今天要做」「AI 已处理」；提醒区「暂无提醒」，与主栏无重复实体。
- 「今天要做」空态「没有时限内目标」；「AI 已处理」空态「今天还没有处理记录」。
- 从「需要你决定」点击「1 条记忆待确认」立即进入 `/memories?tab=review`（远小于 30 秒），可见待确认 HUANGSHAN 中文复述与已拒绝 TAISHAN（superseded）。
- 后端错误态由既有 Playwright `dashboard shows error state on API failure` 覆盖。

Inbox：本机新库 `POST` 轮询写入 20 封真实邮件，记 **pass**（未测 SMTP）。Work / Desktop 本轮不做。

## 本轮明确没测

- macOS 日用库 soak / 向量对账
- 真 LLM 连续两次敏感工具（二次审批走 mock/单测）
- Telegram、proposed 过期、08:00 晨报、桌面通知横幅
