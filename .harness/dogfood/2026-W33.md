# Dogfood 2026-W33 — 2026-08-16

> 当前一行：`2026-W33-R2 Chat:pass Memory:pass Work:pass Desktop:pass Inbox:pass`
> 对照 `docs/05-engineering/development.md` 自用检查表。不是七天日记，是真实 LLM 走查。

## R2（同日第二轮）

环境：后端 `127.0.0.1:8000`（DeepSeek `deepseek-v4-flash`）。`.env` 已填 Gmail，经 `PUT /api/settings/email` 覆盖库内旧账号后 IMAP/SMTP 测试通过。暗号 `QILIAN-DF-R2-0816`。

soak 终态：messages 25、memories 19、work_items completed 4、inbox_emails 4、CapabilityInvoked 8、notifications 6。

| 闭环 | 标记 | 证据 |
|------|------|------|
| Chat | **pass** | 会话 `863d5872-…`：记住暗号 → `write_file` 弹出 `confirmation_required` → `decision=approve` → `/tmp/pai_debug/dogfood_w33_r2.txt` 落盘 `QILIAN-DF-R2-0816` |
| Memory | **pass** | 抽取 3 条含暗号的 `proposed` claim；search 命中（distance ~0.70 / 0.98）。未确认前新会话 `7d6b47cf-…` **答不出**。对 `c57e6cb4-…`（「QILIAN-DF-R2-0816 is the user's second-round dogfood personal passcode」）`POST /ratify` 后，新会话 `2f3c2c21-…` 答出暗号。R1 的 Memory fail 是没走 Memories 确认，不是向量索引坏了。 |
| Work | **pass** | 目标 `211aa65e-…` → decompose 5 步预览 → 子任务 `407722fc-…` pending→running→completed → 目标 completed progress=1.0 |
| Desktop | **pass** | Vite `:5173`（仅 `localhost` IPv6）+ `npm start` Electron 进程在跑（复用 :8000）。`POST /morning-brief/test?persist=true` 持久化「早安简报」。本侧无法截屏确认 macOS 横幅。测完已结束 Electron/Vite。沙箱内 `electron .` 会因 `protocol` 未定义立刻退出，需本机权限。 |
| Inbox | **pass** | IMAP/SMTP test ok；`POST /inbox/poll` `new_count=4`；列表可见 4 封真实邮件；并产生 1 条「重要邮件」通知。早安简报仍报未读 0（读状态与 poll 口径不一致，不挡 Inbox pass）。 |

### R2 卡点（下周输入）

1. **proposed 不进 Chat 是设计，但日用路径不明显**：抽取噪声大（文件路径、debug identifier 也进 proposed）。用户不点 Memories 确认，Chat 永远「没有这条记忆」。优先降噪 + 让确认入口更近，不要先加新 Fragment。
2. **早安简报信噪**：本轮 `proposed_count=18`，把待确认记忆堆到首页；Inbox 已有 4 封邮件，简报仍写未读 0。
3. **Work 状态机**：`pending → completed` 仍 500。本轮继续走 pending→running→completed。
4. **审批默认 deny**、`SENSITIVE_OPS` `/Users/` 误伤、cryptography 49 无 Intel wheel：未变。
5. **Vite 只绑 IPv6 localhost**：`127.0.0.1:5173` 连不上。Electron 用 `localhost` 所以本轮窗口应能加载；直连 IPv4 会踩坑。
6. 遗留：早期调试目标「列出 docs 目录结构」仍可能 `running`。

### R2 明确没测

- 08:00 定时 Morning Brief（仍是手动 `persist=true`）
- Chat 崩溃续跑（max_retries=1）
- macOS 通知横幅截屏
- SMTP 发信

---

## R1（接手后第一轮，保留对照）

> 一行：`2026-W33 Chat:pass Memory:fail Work:pass Desktop:pass Inbox:blocked`

soak 当时：messages 18、memories 11、work_items completed 2、inbox_emails 0、CapabilityInvoked 6

| 闭环 | 标记 | 证据 |
|------|------|------|
| Chat | **pass** | 会话 `1b8d3b0c-…` 两轮：记住暗号 → `write_file` 弹出 `confirmation_required` → `decision=approve` → `/tmp/pai_debug/dogfood_w33.txt` 落盘 `KUNLUN-DF-20260816` |
| Memory | **fail** | 抽取成功且 search 命中；新会话 `aeab583c-…` 答没有。R2 证明根因是 claim 停在 `proposed`、未 ratify。 |
| Work | **pass** | 目标「dogfood W33：完成一次目标闭环」创建 → decompose → 子任务 pending→running→completed → 目标 completed progress=1.0 |
| Desktop | **pass** | Electron 进程 + 早安简报通知。当时未起 Vite，窗口应空白。 |
| Inbox | **blocked** | 库内旧 Gmail 凭据 IMAP `AUTHENTICATIONFAILED`。inbox_emails=0。 |
