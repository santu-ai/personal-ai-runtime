# Dogfood 2026-W36 — 2026-09-01

> 当前一行：`2026-W36 MCP:pass Memory:pass Approval:pass Telegram(mock):pass Telegram(real):blocked`

## 结果

- **MCP 启动**：`MCP_EXTERNAL_ENABLED=true` 冷启动后健康端点返回 200；
  transport 前挂死/初始化超时由 owner-task 取消回收回归覆盖。Context7/Tavily 改为 lazy。
- **Memory 两轮**：真 LLM 使用
  `QOMOLANGMA-DF-W36-0901-R1 → KUNLUN-DF-W36-0901-R2`。终态仅新值
  `ratified`、旧值 `rejected(reason=superseded)`，无回答复述产生的 proposed。
- **连续审批**：real-backend Playwright 通过“第一次确认 → 重启 backend → 第二次确认 →
  最终文本”，两个文件各写一次；resume-only HTTP 回归验证 post-tool LLM 失败不重放能力。
- **proposed 自动过期**：边界、刚好 30 天、ratified 不变、幂等与系统清理统计通过。
- **Telegram mock**：offset、chat allowlist、确定性入站去重、持久 offset、失败退避、
  taint、scoped auto-reply Approval 审计及 Settings 不回显凭据通过。

## 阻塞

本机未配置 `TELEGRAM_BOT_TOKEN`、`TELEGRAM_CHAT_ID`，且未启用
`BUILTIN_TOOL_CATEGORIES=telegram`，因此手机真实收发记为 **blocked**；未用 mock
冒充产品验收。
