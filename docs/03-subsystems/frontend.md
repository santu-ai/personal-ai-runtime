# 前端子系统

本文档描述 React SPA（`frontend/`）。技术栈：React 19 + Vite 8 + TanStack Query 5 + Zustand 5 + Tailwind v4 + react-router-dom 7。版本以 [`frontend/package.json`](../../frontend/package.json) 为准。

## 启动

[`frontend/src/main.tsx`](../../frontend/src/main.tsx)：

- 挂载 `<RouterProvider router={router} />` 包在 `QueryClientProvider` 内。
- `QueryClient` 默认：`staleTime 30s`、`refetchOnWindowFocus: false`、`retry: 1`。
- `import "./auth"` 在模块加载时跑 `initAuth()`。
- 生产模式注册 Service Worker `/sw.js`（桌面构建跳过）。API 与 WebSocket 为 network-only，不读写 Cache Storage；静态资源 cache-first，HTML network-first。`CACHE_VERSION` 变更会在 activate 时清掉旧缓存。Vitest 用 `src/sw.test.ts` 校验 API 请求不碰 Cache Storage（该文件与 `viteConfig.test.ts` 因 Node API 不纳入 `tsc --noEmit`）。

## 路由

[`frontend/src/router.tsx`](../../frontend/src/router.tsx) 用 `createBrowserRouter`，**所有页面组件懒加载**，嵌套在单个 `Layout` 下：

| Route | 页面文件 | 用途 |
|---|---|---|
| `/`（index） | `pages/ChatPage.tsx` | 聊天首页（无会话）→ `ChatHome`。记忆、目标、收件箱、待审批和待确认记忆都读成功且为空时，仍写「我还不太了解你」和「今天没有待决断事项，开始新对话吧」。其中任一还没读到时，不写这两句。读失败时写出原因（有原文用原文；记忆空白则用「加载记忆失败」，目标用「加载目标失败」，收件箱用「加载收件箱失败」，待审批用「加载待审批失败」，待确认记忆用「加载待确认记忆失败」）并给出「重试」。多处一起失败时，先写记忆、目标、收件箱里先失败的那条。「重试」在没有已读到的提示时拿键盘焦点；这次读取结束前按钮留在页面上，原因也不换成空态。已经读到的提示留着，这次失败不抢走它的焦点。「继续上次」和输入框仍在。摘要平时只占一行。键盘落到这一行时写出整句。鼠标点上去仍是一行。输入框里还没发出的字会留着，离开首页再回来还在。创建还没回来时，输入框不禁用，按钮写成「发送中」，不禁用，标为忙碌，焦点留在上面。占位写成「正在发送…，输入消息仍可改」。输入法还在组字或仍是处理键时按 Enter 不发送。再按 Enter 或再点不会再发一次，首页上另一个「开始对话」也不会发出。失败时已经写的字留着，焦点仍在刚才的输入框或按钮；已经掉到页面空白处时落到输入框。创建过程中又改过的字留下；成功后若还和刚发出的一样，才清掉。成功后若焦点还在「发送」上或落在页面空白处，落到输入框。这一输入框带与时间线相同的焦点环。读屏名字一直是「输入消息」。占位换成发送、生成或待确认时，这个名字不变。占位写成「正在发送…，输入消息仍可改」或「正在生成，输入消息可以先写下一条」时，这一句出现就读出来，等当前这一句说完。不把焦点抢过来。待确认那句不另读。平时的占位不另读。失败仍只走右下角提示。旁边的麦克风平时只画图标，名字在悬停的 `title` 里。键盘落到时写出「语音输入」，图标让开；正在听时写出「停止录音」。鼠标点上去仍是图标。听写中的那一句平时被截短，整句在悬停提示里。键盘落到麦克风时写出整句。鼠标点上去仍是截短的。这一句不单独占一个焦点。空格和回车仍开始或停止。已经把焦点移走时不再抢。点首页上的「开始对话」同样不禁用，标为忙碌，焦点留在上面；这次创建回来之前，输入框也不会再开一份。侧栏「新对话」和这次创建共用一把锁：一边还没回来时，另一边也不会再开一份。收件箱「让 AI 处理」不在这把锁里。失败时焦点仍在刚才的按钮；已经掉到页面空白处时落回去。已经把焦点移走时不再抢。「去审批」仍是链接。首页待确认记忆横幅里，每一条平时只占一行，整句在悬停提示里。键盘落到「确认」或「拒绝」时这一行写出整句。鼠标点上去仍是一行。「确认」和「拒绝」在这次写回来之前不会再发一次，同一条上的另一个也不会发出。按钮不禁用，点到的那个标为忙碌，焦点留在上面。失败时仍留在刚才的按钮。成功并且这一条离开横幅的同一轮，焦点还在原处或落在页面空白处时落到下一条的同一个按钮，不先停在页面空白；没有下一条就落到上一条；都没有但仍看得到「查看全部」就落到这个链接；横幅收起后，输入框还在并且能输入时落到输入框。已经把焦点移走时不再抢。另一条仍可以确认或拒绝 |
| `/chat/:conversationId` | `pages/ChatPage.tsx` | 活跃会话 → `ChatView`。读取消息失败且当前没有消息时，写出原因（有原文用原文，空白则用「加载消息失败」）并给出「重试」，不显示「开始对话」。读取成功且没有消息时仍是「开始对话」。「重试」出现时拿键盘焦点；这次读取结束前按钮留在页面上。从首页带过来、还没发出的那句，要等这次读取成功才发送。工具确认或 ask_user 出现时，焦点落到确认按钮或回答框，不回到已经禁用的输入框。焦点在这张卡片里时，Tab 与 Shift+Tab 留在卡片里，从最后一个控件回到第一个，从第一个回到最后一个。已经禁用或藏在收起的详情里的控件跳过。焦点在卡片容器上时，Tab 进第一个还能用的控件，Shift+Tab 进最后一个。焦点已经在别的控件上时，Tab 不再拉回来。焦点掉到页面空白处、且没有别的对话框时，Tab 回到这张卡片。卡片里已经展开的「查看详细参数」「查看变更」或「查看写入内容」，焦点还在这一段里时，Esc 先收起这一段并落到标题上，不离开卡片。里面的「查看完整内容」先收起外面那一层。输入法还在组字或仍是处理键时不收起。收起之后再按 Esc 才离开卡片，落到「上下文」；上下文已经展开时落到「收起」。点「上下文」打开面板的同一轮，焦点还在这一钮上或落在页面空白处时落到「收起」，不先停在页面空白。点「收起」关掉的同一轮落到「上下文」。面板打开时，焦点在面板里按 Esc 也收起，同一轮落到「上下文」。输入法还在组字或仍是处理键时不收起。焦点在输入框或别的控件上时不收起。已经把焦点移走时不再抢。发出消息之后若多出一条待确认记忆，顶上写出「待确认：…」。这一句出现时读出来，读的是整句，等当前这一句说完。不把焦点抢过来。大约六秒后自己消失，也可以点「关闭」。焦点还在这一条上时按 Esc 也关掉。输入法还在组字或仍是处理键时不关掉。超过 40 个字时平时只写出前 40 个字。键盘落到「关闭」时写出整句，短的那句让开。鼠标点上去仍是前 40 个字，整句在悬停提示里。这一钮平时是叉号。键盘落到时写出「关闭」，叉号让开。鼠标点上去仍是叉号。这一条卸下的同一轮，焦点还在上面或落在页面空白处时，输入框还能输入就落到输入框；待确认时落到回答框或确认按钮；再没有就落到「上下文」，已经展开时落到「收起」。已经把焦点移走时不再抢。不会确认，也不会取消。输入法还在组字或仍是处理键时按 Esc 不离开。回答框里按 Ctrl 或 Cmd + Enter 发送已写好的回答；输入法还在组字或仍是处理键时这一下不发送，也不拦住。正跟在最新消息时，卡片出现后再把记录滚到底，避免卡片占掉高度后看不到刚才那一轮。已经往上翻了不硬拉，跳转写成「↓ 待确认」，点了才回到底部；没有待确认时仍是「↓ 新消息」。这一钮卸下的同一轮，焦点还在上面或落在页面空白处时，有待确认就落到回答框或确认按钮，没有就落到输入框，不先停在页面空白。已经把焦点移走时不再抢。确认、取消和发送回答在这次续写回来之前不会再发一次，旁边的另一个也不会发出。按钮不禁用，点到的那个标为忙碌，焦点留在上面。回答框也不禁用，并带与时间线相同的焦点环。续写失败时卡片留着，ask_user 已经写的回答也留着。焦点还在刚才的按钮上、且这个按钮还能用，就留在那里。已经掉到页面空白处，或还停在已经禁用的按钮上时，同一轮落到刚才的按钮；这个按钮已经不可用时落到回答框。不先停在页面空白。已经把焦点移走时不再抢。点「取消」成功且没有另给出回复时，对话里写出「已拒绝「名字」，没有执行该操作。」。名字和确认卡片相同。没有名字时写出「已拒绝该操作。」。这一句出现时读出来，等当前这一句说完。不把焦点抢过来。按钮上的忙碌不另读。失败仍只走右下角提示。已经有回复时不另读这一句。再取消一次仍再读一遍。待确认卡片出现时读出标题。工具卡等能力策略回来再读，避免先读一遍再补上高风险。向你确认出现就读。标着高风险时再读出「高风险」。标题里没有的那一句操作也读出来。向你确认时读出「需要你补充一点信息」和问题，有补充说明也读出来。等当前这一句说完。不把焦点抢过来。同一张不重复。换一张再读。关掉时不另读。按钮上的忙碌不另读。失败不另读。参数、提示和按钮名字不另读。打开会话时这张还在，也读这一次。单步和多步工具旁边的「执行中」不另读。结果写回来、旁边变成「完成」或「失败」时读出来，名字和这一步按钮上相同，后面接「完成。」或「失败。」。同一轮多出来的几步合成一句。等当前这一句说完。不把焦点抢过来。打开会话时已经有结果的不读。结果还是空的仍当作执行中，不读。用户拒绝、结果标明 denied 的不另读。结果正文不另读。已经读过的这一步不再读一遍。换一条会话不再读上一次的。输入框里还没提交的字按这条会话留着，换一条或离开再回来还在，不会带到另一条。欢迎屏能力胶囊平时只写短名字，将放进输入框的那一句在悬停的 title 里。键盘落到时写出这一句，短名字和图标让开；鼠标点上去仍是短名字。「我记得你」超过 60 个字、欢迎屏和会话里的建议超过 50 个字时，平时只写出前一段，悬停提示是整句。键盘落到时写出整句，鼠标点上去仍是前一段。没超长的建议仍整句都在。点欢迎屏上的能力胶囊、「我记得你」，或会话里的建议时，输入框里已经写的字留下；还是空的（只有空白也算空）才放进这句。焦点在这一轮进输入框，不先停在胶囊上。这一输入框带与时间线相同的焦点环。读屏名字一直是「输入消息」。占位换成发送、生成或待确认时，这个名字不变。占位写成「正在发送…，输入消息仍可改」或「正在生成，输入消息可以先写下一条」时，这一句出现就读出来，等当前这一句说完。不把焦点抢过来。待确认那句不另读。平时的占位不另读。失败仍只走右下角提示。旁边的麦克风平时只画图标，名字在悬停的 `title` 里。键盘落到时写出「语音输入」，图标让开；正在听时写出「停止录音」。鼠标点上去仍是图标。听写中的那一句平时被截短，整句在悬停提示里。键盘落到麦克风时写出整句。鼠标点上去仍是截短的。这一句不单独占一个焦点。空格和回车仍开始或停止。已经把焦点移走时不再抢。提交并写进记录后才清掉。还在生成或有待确认时再发送，不会把还没提交的字清掉。从首页带过来的那句也不会清掉这条会话里还没提交的字。生成还没结束时输入框不禁用，标为忙碌，焦点留在上面。占位写成「正在生成，输入消息可以先写下一条」。点「发送」时焦点留在那个按钮上，按钮改成「取消生成」，不禁用，也标为忙碌。输入法还在组字或仍是处理键时按 Enter 不发送。再按 Enter 或再点不会再发一次。接着写的字留着。失败时焦点仍在输入框；已经掉到页面空白处时落回去。生成结束后，焦点还在输入框或「取消生成」上，或落在页面空白处时，留在输入框。已经把焦点移走时不再抢。待确认时输入框仍禁用，按钮写成「待你确认」，不再写成「思考中」。悬停说明先在上面确认或取消，才能继续发送。占位写成「先在上面确认或取消，暂不能输入消息」。焦点仍交给确认或回答。本对话待确认记忆横幅每一条平时只占一行，整句在悬停提示里。键盘落到「确认」或「拒绝」时这一行写出整句。鼠标点上去仍是一行。「确认」和「拒绝」同样在这次写回来之前不会再发一次，焦点与首页横幅相同。助手回复里没有换行、也没有语言标记的行内代码都带「复制」，长过 50 个字符的也一样：悬停时出现；键盘落到这一钮时也显示，写出「复制」，图标让开，并带与时间线相同的焦点环。鼠标点上去仍是图标。复制成功后这一钮写成「已复制」，键盘落到时也写出「已复制」，大约两秒后回到「复制」。复制失败时不写成「已复制」。行内代码超过 80 个字时，平时仍可能横向溢出。键盘落到这一段时写出整段并换行。空格和回车不把页面滚走，也不触发「复制」。输入法还在组字或仍是处理键时这一下不拦住。不超过 80 个字的不单独占一个焦点。「复制」仍在。鼠标点上去仍不换行，整段在悬停提示里。带语言的成段代码，以及正文里有换行的代码块，右上角也有「复制」：悬停时出现；键盘落到这一钮时也显示，写出「复制」，图标让开，并带与时间线相同的焦点环。鼠标点上去仍是图标。复制的是去掉末尾那一个换行之后的正文。复制成功后这一钮写成「已复制」，键盘落到时也写出「已复制」，大约两秒后回到「复制」。复制失败时不写成「已复制」。成段代码某一行超过 80 个字时，平时仍横向滚动。键盘落到这一段时写出整段并换行。空格和回车不把页面滚走。输入法还在组字或仍是处理键时这一下不拦住。每一行都不超过 80 个字的不单独占一个焦点。鼠标点上去仍要横向滚动，整段在悬停提示里。助手回复下的来源胶囊平时最多约 120px，整句在悬停提示里。有标题时键盘可以落到这一颗，写出整句，并带与时间线相同的焦点环；空格和回车不把页面滚走。输入法还在组字或仍是处理键时这一下不拦住。类型图标平时只画图标。键盘落到这一颗时写出「记忆」「邮件」「目标」或「文档」，图标让开。鼠标点上去仍是图标。没有标题的胶囊仍不可落到，类型写在字里，写成「记忆」「邮件」「目标」或「文档」。鼠标点上去仍是短的 |
| `/goals` | `pages/Goals.tsx` | 目标列表 + 详情。与其他主页面共用 `page-shell`；窄屏选中后只显示详情。列表行是链到 `/goals/:id` 的链接（路径按 id 编码），带与时间线相同的焦点环，正在看的那一行标 `aria-current="page"`。标题平时只占一行。键盘落到这一行时写出整句。鼠标点上去仍是一行。这一行还写出和详情相同的状态：进行中、已暂停或已完成。进行中且超过三天没有活动时再写出已停滞。圆点只是颜色。百分比仍在。窄屏「返回列表」链到 `/goals`。新建、改状态、删除和开始讨论仍是按钮。删除要等这次写成功才关掉。失败时确认框留着。正在删除时，再点删除、取消、Esc 或点外面都不会关掉，也不会再发一次。删除和取消不禁用，删除标为忙碌，焦点留在删除上。删除成功后，确认框卸下的同一轮，焦点还在「删除」上或落在对话框、页面空白处时落到下一行目标，不先停在页面空白；没有下一行就落到上一行；都没有就落到「+ 新建」。已经把焦点移走时不再抢。进度经 `goalProgressPercent`：0–1 乘 100；大于 1 视为已经是百分比，显示不超过 100。列表还在加载时写「加载中…」。读取失败且当前没有目标时，页面上写出原因（有原文用原文，空白则用「加载目标失败」）并给出「重试」，不显示「暂无目标」。读取成功且为空时仍是「暂无目标」。「重试」出现时拿键盘焦点；这次读取结束前按钮留在页面上。已有目标时，失败只走原有提示，列表仍在。新建目标时，输入法还在组字或仍是处理键就按 Enter 不会创建。这次写回来之前不会再开一份。点「取消」或在这一栏按 Esc 收起的同一轮，焦点还在「取消」上、在名称框上，或落在页面空白处时回到「+ 新建」，不先停在页面空白。已经把焦点移走时不再抢。输入法还在组字或仍是处理键时 Esc 不收起。这次创建还没回来时，再点「取消」或按 Esc 不收起，已经写的名称留下。名称输入框带与时间线相同的焦点环，和「创建」都不禁用，「创建」标为忙碌，焦点留在上面。创建失败时名称留着，焦点仍在刚才的输入框或按钮。成功后清掉刚发出的名称并收起这一栏；提交过程中又改过的字留下，这一栏也留着。焦点还在「创建」上、还在名称输入框上，或落在页面空白处时，同一轮落到新目标那一行。列表里还没有这一行时，先落到「+ 新建」，不先停在页面空白；这一行出现后，焦点还在「+ 新建」上或落在页面空白处时再落到这一行。创建还没回来时，名称被清掉也不禁用「创建」。留下的草稿若把名称清掉，「创建」变为不可用的同一轮落到名称输入框。已经把焦点移走时不再抢 |
| `/goals/:goalId` | `pages/Goals.tsx` | 同上，直接打开该目标详情。目标不存在时的「返回列表」也链到 `/goals`。详情读取失败且不是不存在时，写出原因（空白则用「加载目标详情失败」）并给出「重试」，不一直显示「加载中…」。「重试」出现时拿键盘焦点，这次读取结束前按钮留在页面上。不存在仍是「目标不存在」。此时若列表也读取失败且没有目标，列表栏写出原因，不写「暂无其他目标」，且不抢走详情里的焦点。行动步骤还在组字或仍是处理键时按 Enter 不添加。这次写回来之前不会再添加一次。输入框和「添加」不禁用，「添加」标为忙碌，焦点留在上面。创建失败时这一行留着，焦点仍在刚才的输入框或按钮。成功才清掉刚发出的那句；提交过程中又改过的字留下。这一句添加成功时读出来，读的是「已添加」和刚发出的那句，等当前这一句说完。不把焦点抢过来。「添加中...」仍写在按钮上，不另读。失败仍只走右下角提示。从建议里点「添加」不另读这一句。换一个目标不再读上一次的。同一句再添加一次，仍再读一遍。焦点还在「添加」上、还在输入框上，或落在页面空白处时，同一轮落到输入框，不先停在已经禁用的「添加」上。这一输入框和行动步骤勾选带与时间线相同的焦点环。旁边已经写着的步骤名连着这一行。点这个名字会勾上或取消，读屏也读出这个名字。没有名字时仍读出「行动步骤」。已经把焦点移走时不再抢。换一个目标时，没发出的步骤和还没添加的 AI 建议都不带到下一个；拆解结果若在换走之后才返回，也不写进当前目标。「暂停」「完成」「恢复」、勾选行动步骤和「AI 拆解」在这次请求回来之前不会再发一次。控件不禁用，标为忙碌，焦点留在上面。失败时仍留在刚才的控件。暂停成功后，「暂停」和「完成」卸下的同一轮，焦点还在原处或落在页面空白处时落到「恢复」，不先停在页面空白；恢复成功落到「暂停」；完成成功落到「就此目标对话」。已经把焦点移走时不再抢。这次改成的状态出现时读出来，和徽章相同：进行中、已暂停或已完成。进行中且超过三天没有活动时再读出已停滞，中间仍是「 · 」。等当前这一句说完。不把焦点抢过来。失败仍只走右下角提示。打开详情时已经写着的不另读。换一个目标不再读上一次的。再改一次仍再读一遍。勾选成功后焦点留在点到的那一行。再次拆解还没回来时，已经列出的建议留着。失败时也留着。成功后换成这次的结果，焦点留在「AI 拆解」。这次列出的建议出现时读出来，读的是「AI 建议的行动步骤」和每一条，等当前这一句说完。不把焦点抢过来。「AI 拆解中...」仍写在按钮上，不另读。失败仍只走右下角提示。加上一条之后这一句不改短，也不再读一遍。没有建议时不另读。换一个目标不再读上一次的。AI 建议里的「添加」和「全部添加」在这次写回来之前不会再发一次。点了「全部添加」，每一条「添加」也不会再发；某一条还在添加时，「全部添加」也不会发出。另一条仍可以添加。按钮不禁用，点到的那个标为忙碌，焦点留在上面。失败时这一条留着，焦点仍在刚才的按钮；已经掉到页面空白处时落回去。添加成功并且这一条离开建议的同一轮，焦点还在原处或落在页面空白处时落到下一条「添加」，不先停在页面空白；没有下一条就落到上一条；都没有就落到行动步骤输入框。全部添加都成功后，「全部添加」卸下的同一轮，焦点还在「全部添加」上或落在页面空白处时落到行动步骤输入框，不先停在页面空白。有的没加上时，焦点还在「全部添加」上。已经把焦点移走时不再抢。「就此目标对话」在这次创建回来之前不会再开一份。按钮不禁用，标为忙碌，焦点留在上面。失败时仍留在刚才的按钮；已经掉到页面空白处时落回去。已经把焦点移走时不再抢。成功后打开这次对话，不再把焦点抢回来 |
| `/tasks` | `pages/Tasks.tsx` | 任务列表 + 详情。与目标页共用 `page-shell`；窄屏选中后只显示详情，可返回列表。列表分「进行中」「历史」：失败且带 `executable_plan`、且不是简报待办的任务留在「进行中」，行上标「可重新执行」；没有计划的失败、已完成和已取消在「历史」。可创建项目资料简报。新建简报、确认执行、再次运行、定时、验收和返工要等这次写成功才关掉。失败时标题、目标、邮箱选项、资料路径、定时的小时和分钟、验收说明和返工意见都留着。正在提交时，输入框和确认、取消都不禁用，确认标为忙碌，焦点留在上面。标题、定时和邮箱这些输入框，以及目标、资料路径、验收说明、返工意见这些多行框和读取邮箱勾选，带与时间线相同的焦点环。这些单行框里按 Enter 会确认。目标、资料路径、验收说明和返工意见仍是多行，Enter 换行。勾选邮箱也不会确认。输入法还在组字或仍是处理键时这一下不确认。还没填够，或这次写还没回来时，再按也不会再发一次。再点确认、取消、Esc 或点外面都不会关掉，也不会再发一次。对话框后面的「执行」「重新执行」「再次运行」「定时再次运行」「验收」和「返工」同样不禁用，标为忙碌。再点它们不会另开对话框，也不会再发一次。焦点还在这些按钮上时留在上面。失败时焦点仍在刚才的按钮或输入框。提交过程中又改过的字留下；成功后若这些字还和刚发出的一样，才关掉并清掉。已经把焦点移走时不再抢。焦点还在确认上，或落在对话框面板、页面空白处时，同一轮落到这一栏的第一个输入框，不先停在页面空白。留下的草稿若让确认变成不可用，也是这一轮，不先停在已经禁用的按钮上。有评审或已转任务时展示近 30 日首版采纳、返工、已转任务和平均评审耗时，以及按交付执行归因的审批次数、恢复次数和模型成本（已归因金额照常显示，文案是「窗口模型成本」，与单个交付版本上的「模型成本」分开；另有未归因简报调用时标出其金额和次数，例如 `窗口模型成本 $0.0000 · 未归因 $1.5000（1 次）`。读上限打满、`llm_cost` 为 unavailable 时显示「窗口模型成本未分开计」。未归因金额单独不可用时，已有窗口金额后面写「未归因未分开计」）。同一段列出该窗口的 `items`。`items` 为空时不另起列表。读取这份摘要失败且还没有可显示的摘要时，写出原因（有原文用原文，空白则用「加载简报指标失败」）并给出「重试」，不把没读到写成没有评审。读取成功但没有评审、也没有已转任务时，仍不单列这段。「重试」在没有打开任务、且列表自己没有「重试」时拿键盘焦点；打开了任务，或列表正在显示自己的「重试」时，不抢走焦点。这次读取结束前按钮留在页面上。已经显示的摘要在再次读取失败时留着，失败只走提示。非空 `work_id` 打开 `/tasks/:id`（路径按 id 编码），带与时间线相同的焦点环；行上是去掉空白后的 `title`，标题为空时显示「项目简报」。空白 `work_id` 只显示这个标题，不编造链接。列表行是链到 `/tasks/:id` 的链接（路径按 id 编码），带与时间线相同的焦点环，正在看的那一行标 `aria-current="page"`。标题平时只占一行。键盘落到这一行时写出整句。鼠标点上去仍是一行。窄屏「返回列表」链到 `/tasks`。列表还在加载时写「加载中…」。读取失败且当前没有任务时，页面上写出原因（有原文用原文，空白则用「加载任务失败」）并给出「重试」，不显示「暂无任务」。读取成功且为空时仍是「暂无任务」。「重试」出现时拿键盘焦点；这次读取结束前按钮留在页面上。已有任务时，失败只走原有提示，列表仍在 |
| `/tasks/:taskId` | `pages/Tasks.tsx` | 同上，直接打开该任务详情：交付结果优先，可验收/返工；执行日志可折叠。收起时说明平时只占一行。键盘落到这一栏时写出整句。鼠标点上去仍是一行。展开后不再单列这句说明。最近一步输出把空白收成一行之后，超过 240 个字时平时只写出前一段。键盘落到这一段时写出整段。空格和回车不把页面滚走。输入法还在组字或仍是处理键时这一下不拦住。不超过 240 个字的不单独占一个焦点。鼠标点上去仍是前一段，整段在悬停提示里。日志里的 handler 行优先显示调度执行已有的 `error`（超时、死信或重试原因）；该字段为空白时，显示同一次 `ExecuteRequested` 引起的 `ExecuteCompleted.error`（计划内部失败，handler 正常返回：异常文本，或工具步骤 failed/denied 时步骤结果里已有的原因）。空字符串不显示。有 `executable_plan`、且不是简报待办时，状态为 failed，或仍为 running 且当前尝试那条 `ExecuteRequested` 的 handler 已失败或死信，主按钮显示「重新执行」。审批恢复先写请求、handler 再补 `status=running`（`caused_by` 指向该请求）时，这一条仍算当前尝试。这条请求还没有 handler 行时，`handler_execution` 为空；任务仍是 running，不拿上一轮失败把按钮改成「重新执行」。没有计划，或状态为 `waiting_approval`、`cancelled`、`completed` 时，不显示这个「重新执行」按钮。这两种情况下，非空白的 `handler_execution.error` 显示在对应提示旁边；已有交付、执行日志默认折叠时，显示在交付区上方。日志展开后仍显示同一段原文。验收可以填写可选说明，留空不传理由。「已验收」和「已要求返工」的版本在交付区显示该版本非空的 `latest_decision.reason`（空白不显示），版本历史同一行也写出。返工打开带 `reason=rework_restore`。这次 `changes_requested` 之后，或这次打开之后，没有 `ExecuteRequested` 时，当前交付不再显示「已要求返工」，读模型把它呈现为待验收，可以再次验收或返工；决定事件仍在日志里。已经发出 `ExecuteRequested` 的返工仍显示「已要求返工」。收回后若已验收，同一返工幂等键重试不会把页面改回「已要求返工」。有上一版时，交付区显示「相对 vN」：用该版已存储的 findings、sources、limitations、suggested_actions，以及摘要、正文是否变化，对照 `supersedes_delivery_id` 指向的版本现算，不新增事件，也不把上一版全文放进对比结果。第一版或找不到上一版时该字段为 null，不显示这段。结论、限制或待办已有条目差异时，不再另写「正文已更新」；只有正文变了而上述条目没变时才写。交付区列出该版本已存储的 `checks`：`pass` 显示为通过，`fail` 为未通过，`needs_review` 为待判断；非空 `detail` 写在同一行（程序生成的英文说明换成中文，其它原文保留）。没有检查，或条目去掉空白后没有要求、结果和说明时，不显示这段。版本历史同一行写出这三类结果的条数；未通过和待判断带上对应 `criterion`。全部通过时只写通过条数。其它非空结果按原文计数。当前版来自详情里的交付包，历史全文来自单版本读取，字段相同。版本历史里正在看的那一行标出。读取全文时，交付区用转圈占住原来的位置，这一行标为忙碌。失败时在同一处显示原因；「重试」出现的同一轮拿到键盘焦点，不先停在页面空白，这次读取结束前按钮留在页面上。读回来之后这一钮卸下的同一轮，焦点还在原处或落在页面空白处时落到这一版的交付标题，不先停在页面空白。已经把焦点移到版本行或别的控件上就不再抢。再点同一行也会再读一次。已经打开的版本再点不重复请求。只有一版时仍不单列版本历史，交付区下面写「目前只有这一版。」。项目简报还没有任何版本时写「还没有版本历史。」，不另起可点的行。有多版时，方向键在版本行之间移动焦点，Home 与 End 跳到首尾。输入法还在组字或仍是处理键时方向键不移动，也不拦住这一下。组字结束后再按仍移动。回车或空格才打开那一版，方向键不另发读取。点开另一版，或从历史版回到当前版时，交付区滚到滚动容器顶部，从这一版的开头看；版本行上的焦点不动。还在加载或这次读取失败时，同样滚到交付区。刚打开任务，或换一个任务时清掉上一份历史版，都不滚。正文为空或只有空白时写「这一版没有正文」。「相对 vN」用与正文相同的下沉底，长编号在面板内换行。交付区写出该版本的 `model_cost`：恢复次数和已归因模型成本（例如 `恢复 2 · 模型成本 $1.2500`）。金额从该执行最早的 `ExecutionRequested` 起计，没有这条事件时从该版发布时间起。这一行的说法是「模型成本」，不是近 N 日合计的「窗口模型成本」。读上限打满时显示「恢复未分开计」或「模型成本未分开计」，不显示 $0。没有执行 id 时恢复为 0、成本为 $0.0000。未归因金额不写在这一行。有多版时，版本历史同一行用同样的措辞写出该版列表里的 `model_cost`（评审状态之后、验收或返工说明之前）；字段缺失时不写这一段。已完成且已有交付的项目简报可以「再次运行」：仍是这一份任务，不记返工；新版本沿用 `changes_from_previous` 对照当前交付。同一状态下可以「定时再次运行」：填写小时或分钟后，用既有 `set_timer` 把这一份的 `work_id` 写入定时 payload。到点再次运行或打开同一份任务；任务已不在时这次触发仍是普通提醒。不另建简报。结论和建议待办上的 `source_ids`，以及「相对 vN」里的来源编号，共用 [`deliverySourceNav`](../../frontend/src/utils/deliverySourceNav.ts)：`id` 完全相同才命中（`email:m1` 不会点到 `email:m10`）。正在看的这一份（含历史版本的单版本读取）的来源行是滚动目标；切到历史版本时不用最新一版的来源行。结论和建议待办，以及「相对 vN」里当前版本的 `source_ids`（新增结论、改写后的结论、新增待办、更新后的待办）可点，滚到该行；`type=email` 且编号为 `email:{message_id}` 时，同时用既有 `getInboxEmailDetail` 打开收件箱详情，不另开 `/inbox/:id`。这一读失败时，交付区写出原因（有原文用原文，空白则用「加载邮件详情失败」）并给出「重试」，交付留着，来源按钮不改成「加载中...」。同一封还在读时，再点这一封的来源按钮或编号不会再发一次。这些按钮不禁用，标为忙碌，焦点留在点到的那个上。另一封仍可以打开。成功打开后焦点进入对话框，Esc 回到打开前的控件。已经把焦点移走、对话框还没打开时，不再把焦点抢回来源按钮。失败后「重试」仍拿焦点；重试期间这一封的来源按钮也标为忙碌。「重试」出现时拿键盘焦点；这次读取结束前按钮留在页面上，原因也不换成「加载中...」。换一封邮件，或点到不是邮件的来源，就不再沿用上一封的原因。打开后的 AI 摘要和收件箱相同：「AI 正在生成摘要...」出现时读出来，摘要写回来时读这一段，为空时仍写「（无法生成摘要）」。不把焦点抢过来。失败不改回这一句，也不再读一遍。换任务或换正在看的版本同样清掉。右下角提示仍在。文件路径来源行保持纯文本。上一版编号（`previous_source_ids`），以及去掉的结论、去掉的待办、去掉的来源里的来源编号，若仍在正在看的这一份来源行上，走同一套跳转。这些编号若只存在于被比较的上一版、且该来源 `type=email`、编号为 `email:{message_id}`，则打开收件箱详情；页面上没有上一版来源行，所以不滚动。类型来自上一版交付的 `sources`，或 `sources_removed` 上已有的 `type`，不单凭 `email:` 前缀判断。对不上的编号、不在这一份里的文件来源、文件路径，以及「新增来源 / 来源有更新」整行，保持纯文本。正文 `<pre>` 里反引号包住、且与正在看的这一份来源 `id` 完全相同的编号可点，走同一套跳转；反引号里的文件路径和其它文字保持原样。建议待办里非空 `adopted_work_id`（去掉空白后仍非空）的「已转为任务」链到 `/tasks/:id`（路径按 id 编码），焦点环与时间线相同；空白 id 只显示这句，不编造链接。「转为任务」仍是按钮。「完成」「取消」和「转为任务」在这次写回来之前不会再发一次，旁边的执行、验收或返工也不会趁这次写开对话框。控件不禁用，点到的那个标为忙碌，焦点留在上面。失败时仍留在刚才的按钮。完成或取消成功后，这些按钮卸下时，焦点还在原处或落在页面空白处，同一轮落到正在看的任务链接；这条链接藏起来时落到「返回列表」。转为任务成功后，这一条按钮卸下时，同一轮落到这一条「已转为任务」；没有可打开的链接时同样落到正在看的任务。已经把焦点移走时不再抢。验收或返工成功并关掉对话框后，焦点先回到「验收」或「返工」。当前交付刷新后不再是待验收、这两个按钮卸下时，焦点还在原处或落在页面空白处，同一轮落到正在看的任务链接；这条链接藏起来时落到「返回列表」。已经把焦点移走时不再抢。「确认执行」或「再次运行」成功并关掉对话框后，焦点先回到「执行」「重新执行」或「再次运行」。状态刷新后这个按钮卸下时，焦点还在原处或落在页面空白处，同一轮落到正在看的任务链接；这条链接藏起来时落到「返回列表」。已经把焦点移走时不再抢。「完成」「取消」「确认执行」或「再次运行」写成功后，详情里这一行的状态换成新的字时读出来，和页面上相同：待执行、运行中、阻塞、待审批、已完成、失败或已取消。不认识的仍读出原来的字。等当前这一句说完。不把焦点抢过来。打开详情时已经写着的不另读。这一行的字没换就不另读。验收、返工和「转为任务」不另读任务状态这一句。「已设定，将在 … 再次运行」不另读。失败仍只走右下角提示。换一个任务不再读上一次的。同一句再出现一次仍再读一遍。自己刷新改掉的状态不另读。验收或返工写成功后，详情里评审换成新的字时读出来，和页面上相同：已验收或已要求返工。不认识的仍读出页面上的「无交付」。字没换就不另读。这一轮若页面上写出验收说明或返工理由，再读这一句，字和页面上相同。空白不另读。提交过程中说明又改了、对话框还留着时不另读。等当前这一句说完。不把焦点抢过来。打开详情时已经写着的不另读。失败仍只走右下角提示。换一个任务不再读上一次的。同一句再出现一次仍再读一遍。自己刷新改掉的评审或说明不另读。「定时再次运行」成功后按钮还在，焦点留在上面。任务不存在时的「返回列表」链到 `/tasks`。详情读取失败且不是不存在时，写出原因（空白则用「加载任务详情失败」）并给出「重试」，不一直显示「加载中…」。「重试」出现时拿键盘焦点，这次读取结束前按钮留在页面上。不存在仍是「任务不存在」。此时若列表也读取失败且没有任务，列表栏写出原因，不写「暂无其他任务」，且不抢走详情里的焦点 |
| `/inbox` | `pages/Inbox.tsx` | 未读分拣三列（重要 / 需跟进 / 可忽略；卡片上的主题和发件人平时各占一行里的一段。键盘落到「查看」「标记已读」或「让 AI 处理」时写出整句，并改成上下两行。鼠标点上去仍是一行）+ 最近 15 封（仅标题与发件人，未读加粗；每一行是按钮，带与时间线相同的焦点环。主题和发件人平时各占一行里的一段。键盘落到这一行时写出整句，并改成上下两行。鼠标点上去仍是一行。打开后 Esc 回到这一行。同一行还在读详情时再点不会再发一次）+ 最近同步时间/结果/失败原因与「重试同步」。列表还在加载时写「加载中...」。读取失败且当前没有邮件时，页面上写出原因（有原文用原文，空白则用「加载收件箱失败」）并给出「重试」，不显示「还没有同步到邮件」。读取成功且为空时仍是这句。「重试」出现时拿键盘焦点；这次读取结束前按钮留在页面上。已有邮件时，失败只走原有提示，列表仍在。同步失败仍是「重试同步」，和这次列表读取分开。「立即轮询」和「重试同步」在这次同步回来之前不会再发一次；打开页面时已经在同步的那一次也按住这两个按钮。按钮不禁用，标为忙碌，焦点留在上面。失败仍走右下角提示。已经把焦点移走时不再抢。点开一封邮件时，详情读取失败写出原因（有原文用原文，空白则用「加载邮件详情失败」）并给出「重试」，列表留着，不把「查看」改成「加载中...」。「查看」在这次读取回来之前不会再发一次。按钮不禁用，标为忙碌，焦点留在上面。成功打开后焦点进入对话框，Esc 回到这一张的「查看」；打开前焦点已经在别的控件上时，Esc 回到那个控件。已经把焦点移走、对话框还没打开时，不再把焦点抢回「查看」。失败时「重试」仍拿焦点；这次读取结束前按钮留在页面上，原因也不换成「加载中...」。换一封邮件就不再沿用上一封的原因。已经打开的邮件里，「AI 正在生成摘要...」出现时读出来，等当前这一句说完。摘要写回来时读这一段。摘要为空时仍写「（无法生成摘要）」，也读这一句。不把焦点抢过来。关掉时不另读一句完成。失败仍写出原因（空白则用「摘要生成失败」）并给出「重试」，不改回「AI 正在生成摘要...」，也不在旁边再读一遍。邮件详情打开后焦点进入对话框。右上角关掉平时是叉号，键盘落到时写出「关闭」，叉号让开，鼠标点上去仍是叉号。Esc 关闭并回到刚才的控件；摘要失败时「重试」仍拿焦点。打开邮件不自动标已读。「标记已读」和「让 AI 处理」在这次请求回来之前不会再发一次，同一张上的另一个也不会发出。按钮不禁用，点到的那个标为忙碌，焦点留在上面。失败时仍留在刚才的按钮。标记已读成功并且这张离开未读列的同一轮，焦点还在原处或落在页面空白处时落到同一列下一张的「标记已读」，不先停在页面空白；没有下一张就落到这封在最近邮件里的那一行；再没有就回到「立即轮询」。已经把焦点移走时不再抢。「让 AI 处理」仍然只创建一次对话；状态写失败时也只开这一次。创建对话失败且邮件已离开未读列时，焦点按同样的规则在同一轮落下，不先停在页面空白。「查看摘要」同样把焦点放进对话框。右上角关掉平时是叉号，键盘落到时写出「关闭」，叉号让开，鼠标点上去仍是叉号。Esc 关闭并回到刚才的控件。摘要里的链接带与时间线相同的焦点环。摘要里的成段代码，某一行超过 80 个字时，平时仍横向滚动。键盘落到这一段时写出整段并换行。空格和回车不把页面滚走。输入法还在组字或仍是处理键时这一下不拦住。每一行都不超过 80 个字的不单独占一个焦点。鼠标点上去仍要横向滚动，整段在悬停提示里。不另加「复制」。任务页点来源打开同一封邮件时，用的是同一个详情对话框；读失败写在交付区并给出「重试」，见任务页。某一类没有未读时仍写「暂无」 |
| `/memories` | `pages/Memories.tsx` | 记忆列表 + 图谱（含 `?tab=portrait` 画像、`?tab=review` 待确认 triage：筛选/批量确认拒绝）。列表读取失败且当前没有记忆时，写出原因（有原文用原文，空白则用「加载记忆失败」）并给出「重试」，不显示「我还没有记住任何事」。读取成功且为空时仍是这句。这次读取结束前按钮留在页面上，整页也不改回「加载中…」。待确认这次重试同样不把整页改回「加载中…」。画像读取失败且还没有画像时，写出原因（有原文用原文，空白则用「加载画像失败」）并给出「重试」，不一直显示「正在生成你的 AI 画像…」，也不写成「画像尚未建立」。读取成功且没有洞察时仍是「画像尚未建立」。已经生成的画像在再次读取失败时留着，旁边的「重试」带与时间线相同的焦点环。待确认读取失败且还没有这一页时，写出原因（空白则用「加载待确认记忆失败」），不显示「没有待确认的记忆」或「该分类下没有待确认的记忆」。已拒绝读取失败且还没有列表时，写出原因（空白则用「加载已拒绝记忆失败」），不把这一段藏起来；这次「重试」不抢走待确认区的焦点。上方「列表」「待确认」「图谱」「画像」用方向键切换，Home 与 End 跳到首尾，到头就停住。输入法还在组字或仍是处理键时方向键不切换。Tab 只落到当前这一项。切到「待确认」时如果这一页还在读，视图留着，不整页改回「加载中…」，也不写成「没有待确认的记忆」。图谱上的点悬停时写出这条记忆。超过三十个字时悬停只写出前三十个字。键盘落到时写出整句，短的那句让开。鼠标点上去仍是前三十个字。图例写出和列表相同的分类，只写这一张图里有的。不认识的分类仍写出原来的字。没有分类的不单列。圆点只是颜色。悬停或键盘落到时，分类写在这句话上面。读屏读出分类和这句话。焦点仍在圆点上。圆点描边用与时间线相同的焦点色。方向键沿着这些点移动，Home 与 End 跳到首尾；输入法还在组字或仍是处理键时方向键不移动。空格和 Enter 不把页面滚走。图谱读取失败时写出原因（空白则用「加载记忆图谱失败」），不显示「暂无记忆数据可显示」。读取成功且没有节点时仍是这句。「重试」出现时拿键盘焦点；这次读取结束前按钮留在页面上。已经载入的列表在失败时留着。点「来源」打开来源链对话框，焦点进入对话框，Esc 关闭并回到刚才的控件。更新内容超过 60 个字、索引修复失败原因超过 80 个字时，平时只写出前一段。键盘落到这一句时写出整句。不超过的不单独占一个焦点。鼠标点上去仍是前一段，整句在悬停提示里。空格和回车不把页面滚走。输入法还在组字或仍是处理键时这一下不拦住。拒绝和编辑也是这样，焦点落在输入框。编辑里旁边写着的「内容」和「分类」连着这一栏。点这个名字会进去，读屏也读出这个名字。上方记住、拒绝原因、编辑内容和分类带与时间线相同的焦点环。待确认里的分类和排序下拉，以及全选和每一行的勾选，也带同一焦点环。读取失败时对话框留着，写出原因（有原文用原文，空白则用「加载来源链失败」）并给出「重试」，不改回「加载中...」，列表也留着。右下角提示仍在。「重试」出现时拿键盘焦点；这次读取结束前按钮留在对话框里。读取成功且没有事件时仍是「无事件记录」。换一条记忆不再沿用上一条的原因。上方「记住」在输入法还在组字或仍是处理键时按 Enter 不会创建。这次写回来之前不会再发一次。输入框和按钮不禁用，按钮标为忙碌，焦点留在上面。创建失败时这句话留着，焦点仍在刚才的输入框或按钮。成功后清掉刚发出的那句；提交过程中又改过的字留下。这一句记住成功时读出来，读的是「已记住」和刚发出的那句，等当前这一句说完。不把焦点抢过来。「记住中...」仍写在按钮上，不另读。失败仍只走右下角提示。换到别的视图不再读上一次的。同一句再记住一次，仍再读一遍。已经换走之后才记住成功的，也不再读。焦点还在「记住」上或落在页面空白处时，同一轮落到输入框，不先停在已经禁用的「记住」上。已经把焦点移走时不再抢。拒绝时填写的原因、编辑时改过的内容和分类，要等这次写成功才关掉并清掉；失败时对话框和这些字都留着。拒绝原因、编辑内容和分类里按 Enter 会提交。输入法还在组字或仍是处理键时这一下不提交。内容还是空白时编辑不会保存。这次写还没回来时再按也不会再发一次。正在拒绝或保存时，原因、内容、分类、确认和取消都不禁用，确认标为忙碌，焦点留在上面。再点确认、取消、Esc 或点外面都不会关掉，也不会再发一次。失败时焦点仍在刚才的按钮或输入框。提交过程中又改过的字留下；成功后若还和刚发出的一样，才关掉并清掉。已经把焦点移走时不再抢。焦点还在确认上，或落在对话框面板、页面空白处时，同一轮落到这一栏的第一个输入框，不先停在页面空白。留下的草稿若让确认变成不可用，也是这一轮，不先停在已经禁用的按钮上。「忘掉」同样要等成功才关掉；正在忘掉时 Esc 或取消都不会把它关掉，也不会再发一次。忘掉和取消不禁用，忘掉标为忙碌，焦点留在忘掉上。忘掉成功后，确认框卸下的同一轮，焦点还在「忘掉」上或落在对话框、页面空白处时落到下一行的「忘掉」，不先停在页面空白；没有下一行就落到上一行；都没有时，列表回到上方输入框，待确认页回到「待确认」。已经把焦点移走时不再抢。确认和恢复在这次写回来之前不会再发一次，同一行的拒绝也不会打开。按钮不禁用，点到的那个标为忙碌，焦点留在上面。失败时仍留在刚才的按钮；已经掉到页面空白处时回到这个按钮。这一条离开当前列表的同一轮，焦点还在原处或落在页面空白处时落到下一行还能确认或恢复的按钮，不先停在页面空白；没有下一行时，待确认页回到「待确认」，列表页回到上方输入框。已经把焦点移走时不再抢。另一行仍可以确认或恢复。批量确认和批量拒绝同样不会再发一次，另一个也不会发出。没选中时这两个仍不可用。点到的那个不禁用，标为忙碌，焦点留在上面。失败时选中留着，焦点仍在刚才的按钮；已经掉到页面空白处时回到这个按钮。成功后若焦点还在这个按钮上或落在页面空白处，同一轮落到还没处理的「确认」；没有就回到「待确认」，不先停在已经禁用的批量按钮上。已经把焦点移走时不再抢。「继续聊」在这次创建回来之前不会再开一份，另一条的「继续聊」也不会发出。按钮不禁用，点到的那个标为忙碌，焦点留在上面。失败时仍留在刚才的按钮；已经掉到页面空白处时落回去。已经把焦点移走时不再抢。成功后打开这次对话，不再把焦点抢回来 |
| `/dashboard` | `pages/Dashboard.tsx` | 「今天」工作台：三栏「需要你决定 / 今天要做 / AI 已处理」+ 近 7 日建议采纳 + 近 7 日与前 7 日对比 + 无法进栏的导流提醒（`?tab=trust` 信任报告、`?tab=monitors` 收件箱/网页监控）。整页还没有任何一块数据时，读取失败写出原因（有原文用原文，空白则用「无法连接到后端服务，请确认后端已启动」）并给出「重试」，不一直停在「加载中...」。「重试」出现时拿键盘焦点，这次读取结束前按钮留在页面上，原因也不换成「加载中...」。已经有任意一块数据时，失败仍只走提示，页面留着。「刷新」在这次读取回来之前不会再发一次；已经有读取在进行时再点也不会另发。按钮不禁用，标为忙碌，焦点留在上面。焦点已经掉到页面空白处时回到「刷新」。已经把焦点移走时不再抢。「执行」面板除最近失败外，还列出 `failed` 里其余失败（最新在前），跳过与最近失败相同的 `id`，以及已在死信列表中的 `id`。最近失败、其余失败、重试中与死信仅在读模型已有 `work_id` 时链到 `/tasks/:id`；没有该字段的行保持纯文本，`correlation_id` 不当作任务 id。同一条执行（相同 `id`）不会在最近失败、其余失败和死信里重复出现。重试中的错误写在行内。失败、重试和死信平时只占一行，整句里的错误在悬停提示里。键盘落到这一行时写出整句。没有任务链接但有错误的那一行也可以落到，空格和回车不把页面滚走；输入法还在组字或仍是处理键时这一下不拦住。没有错误、也没有任务链接的行仍不可落到。鼠标点上去仍是一行。返回的定时条目或可再次运行的简报非空时，显示「定时」（各最多 5 条；多出来的定时只写「另有 N 个定时任务」）：定时行只在 payload 里已有 `work_id`（没有该键时用 `action_id`）且任务仍在时链到 `/tasks/:id`，否则纯文本；定时器 id 与 `correlation_id` 不当作任务 id。定时行平时只占一行。键盘落到这一行时写出整句。没有任务链接的也可以落到，空格和回车不把页面滚走；输入法还在组字或仍是处理键时这一下不拦住。鼠标点上去仍是一行。同一份简报链到该任务，再次运行后的新版本对照当前交付。带 `related_type=work_item` 的提醒在详情里打开 `/tasks/:id`。点开提醒时焦点进入详情对话框，Esc 关闭并回到刚才那一行。未读且已持久化的提醒会标成已读，这一行变淡，侧栏未读数一起刷新。标已读失败时这一行保持未读。临时推送（`source` 为 `live`，或 id 以 `live-` 开头）不标已读。类型 `reminder` 在详情里写成「提醒」。提醒行带与时间线相同的焦点环。正文平时最多两行。键盘落到这一条时写出整段。鼠标点上去仍是两行。今天页「运行状况」同样带这圈环。信任报告（`?tab=trust`）读取失败且还没有报告时，写出原因（有原文用原文，空白则用「加载信任报告失败」）并给出「重试」，不一直显示「正在生成信任报告…」，也不把各项计数写成 0。读取成功后仍是这份报告。「重试」出现时拿键盘焦点，这次读取结束前按钮留在页面上。已经生成的报告在再次读取失败时留着。「重试索引」在这次写回来之前不会再发一次。按钮不禁用，标为忙碌，焦点留在上面。失败时原因留在报告里，焦点仍在刚才的按钮。成功并且这一条离开失败列表后，焦点还在原处或落在页面空白处时落到下一条「重试索引」；没有下一条就落到上一条；都没有就落到「返回今日」。已经把焦点移走时不再抢。另一条仍可以重试。信任报告「需要审批」里，非空 `id` 的待审批行打开 `/approvals`；没有 `id` 的行保持纯文本，`correlation_id` 不当作链接。操作名和流程说明平时只占一行。键盘落到有 `id` 的这一行时写出整句。没有 `id`、开不了审批的操作名也可以落到，空格和回车不把页面滚走；输入法还在组字或仍是处理键时这一下不拦住。鼠标点上去仍是一行。记忆索引修复失败的编号平时只占一行，键盘落到「重试索引」时写出整句。最常用工具和被拦截工具的名字平时只占一行，键盘落到时写出整句，空格和回车不把页面滚走，输入法处理键同样不拦住。鼠标点上去仍是一行。今日三栏里打开审批、收件箱、记忆、目标或仪表盘的行，以及「查看全部目标」「还有 N 个目标」，是链接。这三栏里的标题平时只占一行。键盘落到这一行时写出整句。鼠标点上去仍是一行。「需要你决定」里，待审批写成和审批页相同的中文工具名，空白仍是「待审批操作」。邮件标题旁写出「重要」或「需跟进」，和收件箱一样。这三种图标平时只画图标。键盘落到这一行时写出「审批」「记忆」或「邮件」，图标让开。鼠标点上去仍是图标。执行、定时和这些审批行的链接带上与时间线相同的焦点环；需要截断的文字放在链接里面，焦点环留在链接上。没有 `work_id` 或审批 `id` 的行仍是纯文本。监控页（`?tab=monitors`）读取规则失败且还没有列表时，写出原因（有原文用原文，空白则用「加载监控规则失败」）并给出「重试」，不显示「暂无邮件规则」或「暂无网页监控」。读取成功且为空时仍是这两句。「重试」出现时拿键盘焦点；这次读取结束前按钮留在页面上。已经载入的规则在失败时留着。名称和网页地址平时只占一行。键盘落到这一行的「停用」「启用」或「删除」时写出整句。鼠标点上去仍是一行。「添加过滤器」「添加网页监控」「停用」「启用」和「立即检查」在这次写回来之前不会再发一次，旁边的另一个也不会发出。控件不禁用，点到的那个标为忙碌，焦点留在上面。名称、发件人、主题、网页名称、地址或间隔里按 Enter 也会添加。输入法还在组字或仍是处理键时不添加。还没填够时不添加。这次写还没回来时再按也不会再发一次。焦点还在输入框上就留在这里。失败时名称、发件人、主题、网页名称、地址和间隔都留着，这些输入框带与时间线相同的焦点环，焦点仍在刚才的按钮。添加成功后，焦点还在原处或落在页面空白处时，同一轮落到新的一行的「停用」，不先停在已经禁用的「添加过滤器」或「添加网页监控」上；没有这个按钮就落到名称输入框。停用、启用和立即检查成功后焦点留在点到的按钮上。立即检查写出「检查完成：…」时，读屏等这一句说完再读，不把焦点抢过来。已经把焦点移走时不再抢。点「删除」先打开确认框，这次写成功才关掉。失败时确认框留着，焦点仍在「删除」上。正在删除时，再点删除、取消、Esc 或点外面都不会关掉，也不会再发一次。删除和取消不禁用，删除标为忙碌。成功后焦点还在「删除」上，或落在对话框、页面空白处时，落到下一行的「删除」；没有下一行就落到上一行的「删除」；都没有就落到这一段的名称输入框。已经把焦点移走时不再抢。今日三栏还在读取待审批、待确认记忆、收件箱或目标时，不写「今天暂无紧急事项」。其中一栏读取失败且还没有条目时，写出原因（有原文用原文；待审批空白则用「加载待审批失败」，待确认记忆用「加载待确认记忆失败」，收件箱用「加载收件箱失败」，目标用「加载目标失败」，通知用「加载提醒失败」）并给出「重试」，不写「今天暂无紧急事项」「没有待决事项」「没有时限内目标」或「今天还没有处理记录」。读取成功且该栏为空时仍是这些句子。「重试」出现时，页面上第一处拿键盘焦点；这次读取结束前按钮留在页面上。已经载入的条目在失败时留着。「AI 给你的提醒」在通知读取失败且还没有通知时同样写出原因，不写「暂无提醒」。读取成功且为空时仍是「暂无提醒」。从今天点「信任」「监控」或建议采纳进入后，焦点落到「返回今日」。Esc 或点「返回今日」回到今天，并回到刚才那个控件。直接打开 `?tab=trust` 或 `?tab=monitors` 时不抢焦点；这时 Esc 回到「信任」或「监控」。输入法还在组字或仍是处理键时按 Esc 不离开。输入框、多行文本或下拉里按 Esc 不离开。信任报告这次读取失败时「重试」仍拿焦点。回到今天时，如果页面上的「重试」已经拿了焦点，就不再抢到刚才的控件。「运行状况」默认收起。展开后，焦点还在这一栏里按 Esc 收起，并留在「运行状况」上。输入法还在组字或仍是处理键时不收起。没展开时 Esc 不收起 |
| `/settings` | `pages/Settings.tsx` | LLM/邮件/MCP/数据设置。LLM、邮箱和人设旁边已经写着的名字连着这一栏。点这个名字会进到输入框或下拉，读屏也读出这个名字。整页配置读取失败时写出原因（有原文用原文，空白则用「无法加载已保存的配置」）并给出「重试」，不一直显示「加载设置…」。「重试」出现时拿键盘焦点，这次读取结束前按钮留在页面上。MCP 服务器列表读取失败且还没有状态时，写出原因（有原文用原文，空白则用「加载 MCP 服务器失败」）并给出「重试」，不写成「MCP 未启用或连接信息不可用」或「暂无 MCP 服务器」。未启用仍是前一句。已启用且服务器为空时仍是「暂无 MCP 服务器」。服务器名称平时只占一行。键盘落到这一名称时写出整句。空格和回车不把页面滚走。输入法还在组字或仍是处理键时这一下不拦住。鼠标点上去仍是一行。空白名称不单独占一个焦点。MCP 市场读取失败且还没有列表时，写出原因（空白则用「加载 MCP 市场失败」），不显示「暂无可用 MCP 服务器」。读取成功且为空时仍是这句。「安装」在这次写回来之前不会再发一次，旁边另一个「安装」也不会发出。按钮不禁用，点到的那个标为忙碌，焦点留在上面。失败仍走右下角提示（有原文用原文，空白则用「安装失败」），不弹窗，焦点仍在刚才的按钮。成功后这一行写成「已安装」，并写「"名称" 已安装。重启后端后生效。」。焦点还在原处或落在页面空白处时，同一轮落到下一条「安装」；没有下一条就落到上一条；都没有就落到这句说明，不先停在已经禁用的「已安装」上。已经把焦点移走时不再抢。本来已经安装的仍不可再装。市场说明平时只占一行。键盘落到这一句，或落到这一行的「安装」时写出整句。已经安装、按钮不可用时，仍可以落到这一句。空格和回车不把页面滚走。输入法还在组字或仍是处理键时这一下不拦住。鼠标点上去仍是一行。空白说明不单独占一个焦点。市场名称平时只占一行。键盘落到这一名称，或落到这一行的「安装」时写出整句。已经安装、按钮不可用时，仍可以落到这一名称。空格和回车不把页面滚走。输入法还在组字或仍是处理键时这一下不拦住。鼠标点上去仍是一行。空白名称不单独占一个焦点。收起的分区说明平时只占一行。键盘落到这一栏时写出整句。鼠标点上去仍是一行。展开后不再单列这句说明。空格和回车仍展开或收起。系统人设读取失败且还没有配置时，写出原因（空白则用「加载人设配置失败」），不展示空白表单。「重试」出现时拿键盘焦点；这次读取结束前按钮留在页面上。能力策略读取失败且还没有分级时，写出原因（有原文用原文，空白则用「加载能力策略失败」）并给出「重试」，不一直显示「加载策略中…」，也不写成「（无）」。读取成功后仍是自动执行、需要确认和禁止。「重试」出现时拿键盘焦点，这次读取结束前按钮留在页面上。已经载入的配置或列表在失败时留在页面上。Telegram 网关读取失败且还没有状态时，写出原因（有原文用原文，空白则用「加载 Telegram 状态失败」）并给出「重试」，不一直显示「加载 Telegram 状态…」。读取成功后仍是轮询开关和连接状态。这次「重试」出现时拿键盘焦点，读取结束前按钮留在页面上。配置里的输入框和密码框带与时间线相同的焦点环。Telegram 的两个开关和 LLM「启用此 Provider」勾选也带同一焦点环。「显示密码」和「隐藏密码」平时只画眼睛。键盘落到时写出这个名字，图标让开。鼠标点上去仍是图标；已保存的密钥悬停仍写「已保存的密钥无法查看原文」。点「显示密码」或「隐藏密码」时，焦点还在这一栏，就在换掉输入框的同一轮把光标放回去，不先停在页面空白。焦点在眼睛按钮或别的控件上时不再抢。「保存 LLM 配置」和「保存邮箱配置」在这次写回来之前不会再发一次。按钮不禁用，标为忙碌，焦点留在上面。成功后在按钮旁写「已保存」；再改表单就清掉这句。保存还没回来时如果表单又改了，回来后也不写「已保存」。保存失败仍只走右下角提示，不写「已保存」，焦点仍在刚才的按钮。已经把焦点移走时不再抢。LLM 某一家「测试」在这次回来之前不会再发一次，另一家的「测试」也不会发出。点到的那家不禁用，标为忙碌，焦点留在上面。成功后在这一家下面写「连接正常」，读屏等这一句说完再读，不把焦点抢过来；失败仍走提示，不写「连接正常」，焦点仍在刚才的按钮。再改这一家的字段，或测试还没回来时改了字段，都不写「连接正常」。已经把焦点移走时不再抢。邮箱「测试连接」仍用 IMAP / SMTP 结果。两边都正常时，读屏等这一句说完再读；失败仍只走右下角提示，不把这两枚结果再读一遍。这次测试回来之前不会再发一次。按钮不禁用，标为忙碌，焦点留在上面。失败仍走右下角提示，焦点仍在这个按钮。已经把焦点移走时不再抢。Telegram「保存」和「立即轮询」在这次请求回来之前不会再发一次，旁边的另一个也不会发出。按钮不禁用，点到的那个标为忙碌，焦点留在上面。保存成功写「已保存」，读屏等这一句说完再读；保存还没回来时如果开关又改了，回来后不写「已保存」。保存失败把原因写在旁边，并打断当前这一句，焦点仍在「保存」。轮询成功同样等这一句说完再读，失败则打断。不把焦点抢过来。未启用时「立即轮询」仍不可用。正在轮询时如果关掉开关，结束后焦点还在这个按钮或落在页面空白处时，同一轮落到「保存」，不先停在已经禁用的「立即轮询」上。已经把焦点移走时不再抢。系统人设「保存」和「重置」在这次写回来之前不会再发一次，另一栏的保存和重置也不会发出。还不是自定义时「重置」仍不可用，其余按钮不禁用，点到的那个标为忙碌，焦点留在上面。失败时写「保存失败」或「重置失败」，并打断当前这一句，这一栏的字留着，焦点仍在刚才的按钮。保存成功写「已保存」，读屏等这一句说完再读。保存还没回来时如果这一栏又改了字，回来后不写「已保存」，改过的字留下。重置成功后这一栏回到读取到的默认，并写「已重置为默认」，读屏等这一句说完再读。重置还没回来时如果又改了字，不覆盖这一栏，也不写「已重置为默认」。重置成功后，焦点还在「重置」上或落在页面空白处时，同一轮落到这一栏的输入框，不先停在已经禁用的「重置」上。这两栏输入框，以及 LLM 配置里的默认 Provider 和类型下拉，带与时间线相同的焦点环。已经把焦点移走时不再抢。导入备份、覆盖导入和加密导入在这次写回来之前不会再发一次。这三个文件框留在 Tab 顺序里，键盘落到时焦点环画在看得见的按钮上。文件框不因为这次写入而禁用，标为忙碌，焦点留在文件框上。还没写对确认句时覆盖导入仍不可用；还没写密码时加密导入仍不可用。解析失败或导入失败时焦点仍在文件框。覆盖导入成功后若清掉确认句，或加密导入成功后若清掉没改过的密码，焦点还在文件框上时落到旁边的输入框。已经把焦点移走时不再抢。销毁全部数据先打开确认框，要等这次写成功才关掉。失败时确认框留着。正在销毁时，再点销毁、取消、Esc 或点外面都不会关掉，也不会再发一次。对话框里的销毁和取消不禁用，销毁标为忙碌，焦点留在销毁上。后面的「销毁全部数据」同样不禁用，标为忙碌。成功后写出「数据已销毁，请重新启动应用」，读屏等这一句说完再读，焦点仍留在「销毁全部数据」。 |
| `/approvals` | `pages/Approvals.tsx` | 审批队列。`ask_user` 必须先填写文本再「发送回答」，经 chat resolve 续写同一工具环；取消不带回答。非空 `task_id`（去掉空白后仍非空）打开 `/tasks/:id`（路径按 id 编码）；空白、null 或只有空白的 `task_id` 保持纯文本，`correlation_id` 不当作任务 id。列表还在加载时写「加载中…」。读取失败且当前没有待审批项时，页面上写出原因（接口错误原文，空白则用「加载审批列表失败」）并给出「重试」，不显示「暂无待审批项」或「所有高风险操作已处理完毕」。读取成功且为空时仍是这两句。「重试」出现的同一轮拿到键盘焦点，不先停在页面空白；这次读取结束前按钮留在页面上。已有待审批项时，失败只走原有提示，列表仍在。批准、拒绝、发送回答或取消在这次请求回来前不会再发一次，这张卡片上的另一个也不会发出。按钮和回答框不禁用，点到的那个标为忙碌，焦点留在上面。还没写回答时「发送回答」仍不可用。回答框带与时间线相同的焦点环。失败时 ask_user 已经写的回答留着，提交过程中又改过的字也留着，焦点仍在刚才的按钮。焦点已经掉到页面空白处时回到刚才的按钮。拒绝或批准后还留在这一页时，焦点还在点到的按钮上或落在页面空白处时落到下一张还能用的主按钮（还没写回答就落到回答框）；没有下一张时，这一张卸下的同一轮回到「刷新」，不先停在页面空白。已经把焦点移到别的控件上时，不再抢回来。续写成功并打开对话时，不再把焦点留在审批页。「刷新」在这次读取回来之前不会再发一次。按钮不禁用，标为忙碌，焦点留在上面。已经把焦点移走时不再抢。卡片上要做的那一句平时只占一行，整句在悬停提示里。键盘落到这一句时写出整句，空格和回车不把页面滚走；输入法还在组字或仍是处理键时这一下不拦住。鼠标点上去仍是一行。「查看详细参数」「查看变更」「查看写入内容」展开后，焦点在这一段里按 Esc 收起并落到标题上。里面的「查看完整内容」先收起外面那一层。输入法还在组字或仍是处理键时不收起。不会批准，也不会拒绝。「查看详细参数」超过 5 行或某一行超过 80 个字时平时限高。键盘落到这一段时写出整段，空格和回车不把页面滚走，也不收起。输入法还在组字或仍是处理键时这一下不拦住。不超过的不单独占一个焦点。鼠标点上去仍是限高，整段在悬停提示里。写入或变更的「查看完整内容」超过大约十行时同样写出整段，短的不单独占一个焦点 |
| `/timeline` | `pages/Timeline.tsx` | 人生事件时间线。非空 `work_id`（去掉空白后仍非空）的事件描述打开 `/tasks/:id`（路径按 id 编码）；空白、null 或只有空白保持纯文本。不读 `payload_snippet`，也不把 `correlation_id` 当成任务 id。标题在加载、为空和失败时都留着。加载时写「加载中…」。还没有事件时写「还没有任何事件」，并说明开始对话或创建目标后会出现记录。首次读取失败时写出原因（有原文用原文，空白则用「加载时间线失败」）并给出「重试」，不写成还没有事件。已经列出的事件若继续加载失败，仍留在页面上，同一处重试下一页。「重试」出现时拿键盘焦点，这次读取结束前按钮留在页面上。「加载更多」在这次读取结束前不会再发一次，按钮留着并标为忙碌；焦点还在这个按钮上就留在这里。已经没有更早的记录时，「加载更多」卸下的同一轮，焦点还在这个按钮上或落在页面空白处时落到新出现的任务链接，不先停在页面空白；没有这样的链接就落到「已经是最早的记录」。事件列表、空态和「已经是最早的记录」带与任务链接相同的焦点环。继续加载失败时，这一钮卸下的同一轮落到「重试」。首次失败后重试成功，这一钮卸下的同一轮落到第一条任务链接；没有链接但还有下一页就落到「加载更多」；只有不能打开的事件就落到事件列表；仍然没有事件就落到空态。已经把焦点移到别的控件时不再抢 |

## 认证

[`frontend/src/auth.ts`](../../frontend/src/auth.ts)：

**Token 解析顺序**：`import.meta.env.VITE_AUTH_TOKEN`（构建/env）→ `localStorage["auth_token"]` → 无。选中 token 在 React 挂载前经 `setAuthToken()` 推入 API client。`initAuth()` 在模块 import 时调用。

## API Client 分层

[`frontend/src/api/`](../../frontend/src/api/) 是分层结构：

```
core.ts        ← fetch 包装 + auth header + ApiError
   ↑
chat.ts, system.ts, workItems.ts, memory.ts, inbox.ts,
settings.ts, telemetry.ts, approvals.ts,
notifications.ts, portrait.ts, trustReport.ts,
connectors.ts, monitors.ts, timeline.ts
   ↑
client.ts      ← barrel 再导出
types.ts       ← 共享 TS 接口
```

### core.ts

[`frontend/src/api/core.ts`](../../frontend/src/api/core.ts)：

- `API_BASE = "/api"` — **始终相对**。开发经 Vite proxy；生产同源部署。
- Auth header：`Authorization: Bearer <token>`（仅当 token 设置）。
- 401 → 抛 `ApiError`（中文消息「认证失败，请检查 AUTH_TOKEN 与 VITE_AUTH_TOKEN 是否一致」）。
- `ApiError` 带 `.status` 字段。

### 前端如何到达后端

**开发模式** — Vite proxy（[`frontend/vite.config.ts`](../../frontend/vite.config.ts)）：

```
"/api" → http://${API_HOST}:${API_PORT}   (changeOrigin: true)
"/ws"  → http://${API_HOST}:${API_PORT}   (ws: true, changeOrigin: true)
```

`API_HOST`/`API_PORT` 来自 `process.env.VITE_API_HOST`/`VITE_API_PORT`（默认 `127.0.0.1`/`8000`）。配置从**仓库根** `.env`（`envDir: rootDir`）读取——与后端 `AUTH_TOKEN` 同一个文件。Dev server 默认绑 `127.0.0.1`（避免 Vite 同源代理把无认证 API 暴露到局域网）。仅当同时设置 `VITE_DEV_LAN=1` 与非空 `VITE_AUTH_TOKEN` 时才监听所有网卡。

**生产模式** — `API_BASE = "/api"` 相对，要求同源部署同时服务静态前端与 API。Docker Compose 用 Caddy 把 `/`、`/api/*`、`/ws` 挂在同一入口（见 [deployment.md](../05-engineering/deployment.md)）。

### SSE 聊天流

[`frontend/src/api/chat.ts`](../../frontend/src/api/chat.ts) 的 `sendMessage()` 开流式 POST，手动解析 SSE：

- 读 `data: ` 行，解码 JSON `StreamEvent`。
- 事件类型：`text_delta`、`tool_call_start`、`tool_result`、`confirmation_required`、`sources`、`done`、`error`、`ping`（跳过）。
- 30s 空闲超时中止 reader。
- Auth header 直接附加（不走 `request()`）。

## 状态管理

### Zustand stores（[`frontend/src/stores/`](../../frontend/src/stores/)）

- **`chatStore.ts`** — 全局聊天 UI 状态：`conversations`、`activeConversationId`、`pendingPrompt`（被 `useQuickChat` 用于播种新聊天）。
- **`errorStore.ts`** — 全局错误/toast 队列：`errors`（上限 5）、`backendUnavailable` 标志（`Layout` 初始健康检查失败时翻转）、`addError`/`dismissError`/`setBackendUnavailable`/`clearErrors`。`addError` 同时 `console.error`，方便在浏览器 / Electron DevTools 里对照右下角 toast。

### TanStack React Query

主要用于 server cache：

- `useDashboard`（[`hooks/useDashboard.ts`](../../frontend/src/hooks/useDashboard.ts)）— 7 个并行查询（`costSummary`/`costByModel`/`toolSummary`/`memoryStats`/`health`/`notifications`/`dashboard`），全部 `refetchInterval: 60_000`、`staleTime: 30_000`、`retry: 1`。建议采纳与周期对比是页面上另两个查询，不在这 7 个里面。
- `useMemoriesGroupedQuery`（[`hooks/useMemoriesQuery.ts`](../../frontend/src/hooks/useMemoriesQuery.ts)）— query key `["memories","grouped", filters]`，支持 `conversationId` / `source` 过滤当前会话 proposed；`staleTime: 10s`。
- 共享 query key 集中在 [`hooks/useWsInvalidationBridge.ts`](../../frontend/src/hooks/useWsInvalidationBridge.ts) 的 `queryKeys`：`memories`、`memoriesGrouped`、`goals`、`tasks`、`inbox`、`dashboard`、`trustReport`、`approvals`、`timeline`、`conversations`、`notifications`、`governance`，以及 settings / portrait / mcp 相关键。

### WebSocket 失效桥

[`hooks/useWsInvalidationBridge.ts`](../../frontend/src/hooks/useWsInvalidationBridge.ts)：轻量 pub/sub。`useNotifications`（持有 WS）对每个合法 payload 调 `dispatchWsEvent(raw)`，桥订阅并失效 React Query cache：

- `memory_changed` → 失效 `memories`、`dashboard`、`portrait`、`trustReport`、`timeline`、`governance`
- `goal_changed` → 失效 `goals`、`tasks`、`dashboard`、`timeline`、`trustReport`、`portrait`
- `approval_changed` → 失效 `approvals`、`tasks`、`trustReport`、`dashboard`、`governance`（今日采纳卡跟着刷新）
- `notification` → 失效 `notifications`、`dashboard`、`trustReport`；类型名含 inbox/email 时再失效 `inbox`，含 goal 时再失效 `goals`
- 其他 `type` 忽略

`Layout` 根挂载一次。

## 自定义 Hooks

| Hook | 文件 | 职责 |
|---|---|---|
| `useChatMessages` | [`useChatMessages.ts`](../../frontend/src/hooks/useChatMessages.ts) | 加载消息、管理流式状态、解析 tool calls/sources/inbox 摘要、驱动发送循环 |
| `useQuickChat` | [`useQuickChat.ts`](../../frontend/src/hooks/useQuickChat.ts) | 创建会话、导航到 `/chat/{id}`、可选设 pending prompt |
| `useApprovalFlow` | [`useApprovalFlow.ts`](../../frontend/src/hooks/useApprovalFlow.ts) | 管理待工具确认；inflight 审批去重；批准后续写若返回下一审批则立即展示。续写失败时不卸下当前卡片 |
| `useNotifications` | [`useNotifications.ts`](../../frontend/src/hooks/useNotifications.ts) | 持有单 socket WebSocket；指数退避重连（封顶 60s，无次数上限）；`online` 与页面重新可见时立即重连；toast + 实时通知；每 payload 转发到失效桥 |
| `useWsInvalidationBridge` | [`useWsInvalidationBridge.ts`](../../frontend/src/hooks/useWsInvalidationBridge.ts) | 见上 |
| `useDashboard` | [`useDashboard.ts`](../../frontend/src/hooks/useDashboard.ts) | 见上 |
| `useMemoriesGroupedQuery` | [`useMemoriesQuery.ts`](../../frontend/src/hooks/useMemoriesQuery.ts) | 见上 |

## 组件

[`frontend/src/components/`](../../frontend/src/components/)：

- **`ui/`** — 原语：`Button`、`Badge`、`Card`、`Dialog`（这次确认还没回来时，确认和取消都不禁用；确认标为忙碌，焦点留在上面。再点确认、取消、Esc 或点外面都不会关掉，也不会再发一次。单行文字框里按 Enter 会确认。多行框里的 Enter 仍换行，勾选也不确认。输入法还在组字或仍是处理键时这一下不确认。处理键是 isComposing 仍为 false、但 keyCode 为 229 或键名为 Process 的那一下；组字结束后再按仍确认。还没填够，或这次确认还没回来时，再按也不会再发一次。没有单行框的确认框仍要点确认。Tab 与 Shift+Tab 留在对话框里，从最后一个控件回到第一个，从第一个回到最后一个。打开之后，连点落在遮罩上的第二下不关掉；单独点外面仍关掉。输入法还在组字或仍是处理键时按 Esc 不关掉。关掉时焦点在同一轮回到打开前的控件，不先落到页面空白。同一浮层换一条记录时，焦点在外面的控件上，这一轮就进入对话框，不先停在外面的按钮或页面空白；已经在对话框里就不再抢。焦点掉到页面空白时仍回到原来的控件。先进入输入框的，换记录时不再改成对话框本身。邮件详情、摘要、通知详情、快速捕获、记忆的拒绝、编辑和来源链用同一套）、`EmptyState`、`ErrorBoundary`、`Input`（含 `TextArea`、`PasswordInput`。键盘落到时带与时间线相同的焦点环，不再用 `outline-none` 把全局环消掉。「显示密码」和「隐藏密码」平时只画眼睛。键盘落到时写出这个名字，图标让开。鼠标点上去仍是图标；已保存的密钥悬停仍写「已保存的密钥无法查看原文」。点「显示密码」或「隐藏密码」时，焦点还在这一栏，就在换掉输入框的同一轮把光标放回去，不先停在页面空白。焦点在眼睛按钮或别的控件上时不再抢）、`Spinner`、`Disclosure`（设置里收起的分区，以及任务页收起的执行日志，说明平时只占一行。键盘落到这一栏时写出整句。鼠标点上去仍是一行。展开后不再单列这句说明。空格和回车仍展开或收起）。每个有 co-located `.test.tsx`。
- **`layout/`** — `Sidebar.tsx`（聊天列表 + 导航，[`Sidebar.tsx`](../../frontend/src/components/layout/Sidebar.tsx)。收起或窄屏的图标栏平时只画图标，键盘落到导航、「新对话」或通知铃时写出名字，鼠标点上去仍是图标。收起/展开平时也只画图标，键盘落到时同样写出名字。展开的会话列表还在加载时写「加载中…」。读取失败且还没有会话时写出原因（有原文用原文，空白则用「加载对话失败」）并给出「重试」，不显示「暂无对话」。读取成功且为空时仍是「暂无对话」。「重试」出现时拿键盘焦点；这次读取结束前按钮留在列表里。已经列出的会话在失败时留着。会话标题和摘要平时各占一行。键盘落到这一行或旁边的「删除对话」时写出整句。鼠标点上去仍是一行。会话列表里的「删除对话」平时只画垃圾桶。键盘落到时写出「删除对话」，图标让开。鼠标点上去仍是图标。删除对话要等这次写成功才关掉。失败时确认框留着。正在删除时，再点删除、取消、Esc 或点外面都不会关掉，也不会再发一次。删除和取消不禁用，删除标为忙碌，焦点留在删除上。删除成功后，确认框卸下的同一轮，焦点还在「删除」上或落在对话框、页面空白处时落到下一行的「删除对话」，不先停在页面空白；没有下一行就落到上一行；都没有就落到「新对话」。已经把焦点移走时不再抢。对话页的「新对话」在这次创建回来之前不会再开一份，首页「发送」和「开始对话」也不会另开一份。按钮不禁用，标为忙碌，焦点留在上面。失败时仍留在这个按钮；已经掉到页面空白处时落回去。已经把焦点移走时不再抢。成功后打开这次对话，不再把焦点抢回来）、`NotificationBell.tsx`（下拉还在加载时写「加载中…」。读取失败且还没有通知时写出原因（有原文用原文，空白则用「加载通知失败」）并给出「重试」，不显示「暂无通知」。读取成功且为空时仍是「暂无通知」。「重试」出现时拿键盘焦点；这次读取结束前按钮留在下拉里。已经列出的通知在失败时留着。每一条的正文平时最多两行。键盘落到这一条时写出整段。鼠标点上去仍是两行。标题本来就整句都在。下拉打开的同一轮焦点进入面板，不先停在铃上或页面空白。失败「重试」仍在这之后才拿焦点。Tab 和 Shift+Tab 留在面板里，从最后一个控件回到第一个，从第一个回到最后一个。焦点在面板上时，Tab 进第一个还能用的控件，Shift+Tab 进最后一个。已经禁用或藏起来的控件跳过。没有能用的控件时，焦点留在面板上。焦点在面板外时，Tab 回到面板。Esc 关闭并回到铃按钮；输入法还在组字或仍是处理键时按 Esc 不关闭。点外面关掉时焦点留在点到的地方。从一条通知打开详情后，Esc 仍回到铃。「全部已读」在这次写回来之前不会再发一次。按钮不禁用，标为忙碌，焦点留在上面。失败时右下角提示，焦点仍在这个按钮。成功后若没有未读了，「全部已读」卸下的同一轮，焦点还在原处或落在页面空白处时落到第一条通知，不先停在页面空白；没有通知就落到这个面板。已经把焦点移走，或下拉已经关掉时，不再抢）。
- **`chat/`** — `ChatView.tsx`（活跃会话；读取消息失败且还没有消息时写出原因并给出「重试」，不显示「开始对话」；还没提交的字按会话留着；欢迎屏能力胶囊平时只写短名字，键盘落到时写出将放进输入框的那一句，短名字和图标让开，鼠标点上去仍是短名字。「我记得你」超过 60 个字、建议超过 50 个字时，键盘落到时写出整句，鼠标点上去仍是前一段。点建议或欢迎屏胶囊时，已经写的字留下，空的才放进这句，焦点在这一轮进输入框，这一输入框带与时间线相同的焦点环。读屏名字一直是「输入消息」。占位换成发送、生成或待确认时，这个名字不变。占位写成「正在发送…，输入消息仍可改」或「正在生成，输入消息可以先写下一条」时，这一句出现就读出来，等当前这一句说完。不把焦点抢过来。待确认那句不另读。平时的占位不另读。失败仍只走右下角提示。生成还没结束时输入框不禁用，标为忙碌，焦点留在上面，再发送不会再发一次，已经把焦点移走时不再抢；`ProposedMemoryBanner` 只展示当前会话 `source=conv:{id}` 的待确认记忆，toast 文案为「待确认」；发出消息后若多出一条待确认记忆，顶上写出「待确认：…」。这一句出现时读出来，读的是整句，等当前这一句说完。不把焦点抢过来。大约六秒后自己消失，也可以点「关闭」。焦点还在这一条上时按 Esc 也关掉。输入法还在组字或仍是处理键时不关掉。超过 40 个字时平时只写出前 40 个字。键盘落到「关闭」时写出整句，短的那句让开。鼠标点上去仍是前 40 个字，整句在悬停提示里。这一钮平时是叉号。键盘落到时写出「关闭」，叉号让开。鼠标点上去仍是叉号。这一条卸下的同一轮，焦点还在上面或落在页面空白处时，输入框还能输入就落到输入框；待确认时落到回答框或确认按钮；再没有就落到「上下文」，已经展开时落到「收起」。已经把焦点移走时不再抢；「确认」和「拒绝」在写回来之前不会再发一次，按钮不禁用，焦点规则与首页横幅相同）、`ChatHome.tsx`（落地；近况没读全时写出原因并给出「重试」，不显示「我还不太了解你」；横幅仍用全局 proposed 计数；「继续上次」链到该会话。摘要平时只占一行。键盘落到这一行时写出整句。鼠标点上去仍是一行。带地址的提示如「去审批」是链接，开始对话这类仍是按钮；输入框里还没发出的字会留着，这一输入框带与时间线相同的焦点环。读屏名字一直是「输入消息」。占位换成发送、生成或待确认时，这个名字不变。占位写成「正在发送…，输入消息仍可改」或「正在生成，输入消息可以先写下一条」时，这一句出现就读出来，等当前这一句说完。不把焦点抢过来。待确认那句不另读。平时的占位不另读。失败仍只走右下角提示。创建还没回来时不禁用、标为忙碌，再开一份不会发出，侧栏「新对话」也不会另开一份；成功后若还和刚发出的一样才清掉）、`MessageItem.tsx`（正文链接带与时间线相同的焦点环；与当前站点同源的地址走路由，外链仍新开标签。没有换行、也没有语言标记的行内代码（长过 50 个字符的也一样）和成段代码的「复制」悬停时出现，键盘落到时也显示，写出「复制」或成功后的「已复制」，图标让开，并带同一焦点环。鼠标点上去仍是图标。复制成功写成「已复制」，失败不写成已复制。行内代码超过 80 个字时，键盘落到这一段写出整段并换行，空格和回车不把页面滚走，也不触发「复制」；输入法还在组字或仍是处理键时这一下不拦住。不超过的不单独占一个焦点。鼠标点上去仍不换行，整段在悬停提示里。成段代码某一行超过 80 个字时，键盘落到这一段写出整段并换行，空格和回车不把页面滚走；输入法还在组字或仍是处理键时这一下不拦住。不超过的不单独占一个焦点。鼠标点上去仍要横向滚动，整段在悬停提示里。来源胶囊平时最多约 120px，整句在悬停提示里。有标题时键盘落到时写出整句，类型图标让开并写出「记忆」「邮件」「目标」或「文档」。没有标题的仍不可落到，类型写在字里。鼠标点上去仍是短的，图标仍在）、`ToolCallDisplay.tsx`（收件箱结果里，发件人平时被截短，整句在悬停提示里；预览平时最多两行。键盘落到这一行时写出整句，空格和回车不把页面滚走；输入法还在组字或仍是处理键时这一下不拦住。鼠标点上去仍是短的。没有发件人也没有预览的行不可落到。不是收件箱、也不是 JSON 的结果超过 500 个字时，平时仍写出前 500 个字并标出截断。键盘落到这一段时写出整段，这一段的高度限制放开；空格和回车不把页面滚走。输入法还在组字或仍是处理键时这一下不拦住。鼠标点上去仍是前一段，整段在悬停提示里。不超过 500 个字的，以及 JSON，仍直接写出，不单独占一个焦点。展开后的参数里，某一行超过 80 个字时，横向滚动会把后面藏起来。键盘落到这一段时写出整段并换行。空格和回车不把页面滚走，也不收起。输入法还在组字或仍是处理键时这一下不拦住。不超过的不单独占一个焦点。鼠标点上去仍要横向滚动，整段在悬停提示里。多出来的行本来就都在，不因为行数另占一个焦点。这一步已经展开时，焦点在这一步里按 Esc 收起，焦点留在这一步。没展开时不收起。输入法还在组字或仍是处理键时不收起）、`TaskTrack.tsx`（多步轨迹里的工具名平时只占一行。键盘落到这一步时写出整句。鼠标点上去仍是一行。回车和空格仍展开这一步。已经展开时，焦点在这一步里按 Esc 收起，焦点留在这一步。没展开时不收起。输入法还在组字或仍是处理键时不收起。展开后的参数超过 5 行，或某一行超过 80 个字时，平时限高。键盘落到这一段时写出整段，高度限制放开；空格和回车不把页面滚走，也不收起这一步。输入法还在组字或仍是处理键时这一下不拦住。鼠标点上去仍是限高的，整段在悬停提示里。不超过的不另占焦点。展开后的纯文本结果超过 500 个字时，键盘落到这一段写出整段，高度限制放开；空格和回车不把页面滚走，也不收起这一步。输入法还在组字或仍是处理键时这一下不拦住。鼠标点上去仍是前一段。不超过 500 个字的，以及 JSON，不另占焦点）、`ContextPanel.tsx`（活跃目标链到 `/goals/:id`。活跃目标平时只占一行，键盘落到这一行时写出整句。相关记忆平时最多两行，最近工具平时只占一行；键盘落到时写出整句，空格和回车不把页面滚走，输入法还在组字或仍是处理键时这一下不拦住。鼠标点上去仍是短的。这一段还在加载时写「加载中…」。读取失败且还没有目标时写出原因（有原文用原文，空白则用「加载目标失败」）并给出「重试」，不显示「暂无活跃目标」。读取成功且为空时仍是「暂无活跃目标」。「重试」出现时拿键盘焦点；这次读取结束前按钮留着。已经列出的目标在失败时留着。点「上下文」或「收起」卸下这一钮的同一轮，焦点还在上面或落在页面空白处时落到另一钮，不先停在页面空白。面板打开时，焦点在面板里按 Esc 也收起，同一轮落到「上下文」。输入法还在组字或仍是处理键时不收起。焦点在输入框或别的控件上时不收起。已经把焦点移走时不再抢）、`ConfirmationDialog.tsx`（审批模态；`needs_user` 写工具用「建议」话术；`ask_user` 展示问题与文本回答。要做的那一句平时只占一行，整句在悬停提示里。键盘落到这一句时写出整句，空格和回车不把页面滚走；输入法还在组字或仍是处理键时这一下不拦住。鼠标点上去仍是一行。「查看详细参数」「查看变更」「查看写入内容」展开后，焦点在这一段里按 Esc 收起并落到标题上。里面的「查看完整内容」先收起外面那一层。输入法还在组字或仍是处理键时不收起。不会批准，也不会拒绝。「查看详细参数」超过 5 行或某一行超过 80 个字时平时限高。键盘落到这一段时写出整段，空格和回车不把页面滚走，也不收起。输入法还在组字或仍是处理键时这一下不拦住。不超过的不单独占一个焦点。鼠标点上去仍是限高，整段在悬停提示里。写入或变更的「查看完整内容」超过大约十行时同样写出整段，短的不单独占一个焦点。出现时焦点落到确认按钮或回答框。焦点在这张卡片里时，Tab 与 Shift+Tab 留在卡片里；焦点已经在别的控件上时不再拉回来。卡片里已经展开的详情先收起并落到标题上，不离开卡片。输入法还在组字或仍是处理键时不收起。收起之后再按 Esc 才离开卡片，落到「上下文」或「收起」，不确认也不取消。正跟在最新消息时，记录再滚到底；已经往上翻了则显示「↓ 待确认」。点了这一钮，焦点还在上面或落在页面空白处时，同一轮落到回答框或确认按钮；没有待确认就落到输入框。已经把焦点移走时不再抢。确认、取消和发送回答在续写回来之前不会再发一次。按钮不禁用，点到的那个标为忙碌，焦点留在上面。回答留着。回答框里按 Ctrl 或 Cmd + Enter 发送已写好的回答；输入法还在组字或仍是处理键时这一下不发送，也不拦住。失败时焦点还在刚才的按钮上、且这个按钮还能用，就留在那里。已经掉到页面空白处，或还停在已经禁用的按钮上时，同一轮落到刚才的按钮；这个按钮已经不可用时落到回答框。不先停在页面空白。已经把焦点移走时不再抢。点「取消」成功且没有另给出回复时，对话里写出「已拒绝「名字」，没有执行该操作。」。名字和确认卡片相同。没有名字时写出「已拒绝该操作。」。这一句出现时读出来，等当前这一句说完。不把焦点抢过来。按钮上的忙碌不另读。失败仍只走右下角提示。已经有回复时不另读这一句。再取消一次仍再读一遍。待确认卡片出现时读出标题。工具卡等能力策略回来再读，避免先读一遍再补上高风险。向你确认出现就读。标着高风险时再读出「高风险」。标题里没有的那一句操作也读出来。向你确认时读出「需要你补充一点信息」和问题，有补充说明也读出来。等当前这一句说完。不把焦点抢过来。同一张不重复。换一张再读。关掉时不另读。按钮上的忙碌不另读。失败不另读。参数、提示和按钮名字不另读。打开会话时这张还在，也读这一次。单步和多步工具旁边的「执行中」不另读。结果写回来、旁边变成「完成」或「失败」时读出来，名字和这一步按钮上相同，后面接「完成。」或「失败。」。同一轮多出来的几步合成一句。等当前这一句说完。不把焦点抢过来。打开会话时已经有结果的不读。结果还是空的仍当作执行中，不读。用户拒绝、结果标明 denied 的不另读。结果正文不另读。已经读过的这一步不再读一遍。换一条会话不再读上一次的。）、`VoiceInput.tsx`（输入框旁的麦克风平时只画图标，名字在悬停的 `title` 里。键盘落到时写出「语音输入」，图标让开；正在听时写出「停止录音」。鼠标点上去仍是图标。听写中的那一句平时被截短，整句在悬停提示里。键盘落到麦克风时写出整句。鼠标点上去仍是截短的。这一句不单独占一个焦点。空格和回车仍开始或停止）、`CodeBlock.tsx`（懒加载 `react-syntax-highlighter`）。
- **`dashboard/`** — `todayBuckets.ts` 纯前端分桶：需要你决定（待审批 / 待确认记忆 / important·actionable 邮件）、今天要做（3 日内截止或停滞目标）、AI 已处理（当日晨报 / 收件箱摘要 / 目标进展 / 可忽略邮件计数）。`AdoptionSummary` 在今日页展示近 7 日采纳率（工具建议确认 + 记忆确认，数据来自 `GET /api/telemetry/governance`），点击进入信任页。`PeriodComparison` 展示近 7 日与前 7 日的完成目标、完成任务、新邮件和采纳率（`GET /api/dashboard/periods`）；`work_completed_untyped` 只在非零时出现。收回用的 `rerun_restore` / `rework_restore` 不进完成数。`RemindersPanel` 只保留 `reminder` / `url_monitor` / `morning_brief_failed`，先按 `related_id` 去重再截断。`morning_brief` 通知路由到 `/dashboard`。今日三栏标题平时只占一行。键盘落到这一行时写出整句。鼠标点上去仍是一行。「需要你决定」里，待审批写成和审批页相同的中文工具名，空白仍是「待审批操作」。邮件标题旁写出「重要」或「需跟进」，和收件箱一样。这三种图标平时只画图标。键盘落到这一行时写出「审批」「记忆」或「邮件」，图标让开。鼠标点上去仍是图标。今日三栏在待审批、待确认记忆、收件箱、目标或通知还在读取、或读取失败且该栏还没有条目时，不把这一栏写成空；成功的空栏和「暂无提醒」保持原句。
- **`notifications/`** — `NotificationDetailModal.tsx`（打开时焦点进入对话框。右上角关掉平时是叉号，键盘落到时写出「关闭」，叉号让开，鼠标点上去仍是叉号。换一条通知时，焦点在外面的控件上，这一轮进入对话框，不先停在页面空白；已经在对话框里就不再抢。Esc 关闭并回到原控件；`reminder` 写成「提醒」）。
- **`onboarding/`** — `OnboardingWizard.tsx`（首次运行，`localStorage.onboarding_done` 门控。打开后焦点进入面板。Tab 和 Shift+Tab 留在面板里，从最后一个控件回到第一个，从第一个回到最后一个。焦点在面板上时，Tab 进第一个还能用的控件，Shift+Tab 进最后一个。焦点在面板外时，Tab 回到面板。Esc 与「跳过」相同，离开并回到打开前的控件；输入法还在组字或仍是处理键时按 Esc 不离开。检查或创建对话还没回来时 Esc 不离开。点外面不离开。「跳过」和「稍后再说」仍可点。「运行检查」或「下一步」写出的结果，成功等这一句说完再读，失败打断当前这一句，不把焦点抢过来。第三步话题的说明平时只占一行。键盘落到这一题时写出整句。鼠标点上去仍是一行。没有说明的「自由聊几句」不另占一行。回车和空格仍会开始这一题）。

侧栏导航模型（[`Sidebar.tsx`](../../frontend/src/components/layout/Sidebar.tsx)）：

- 分组「概览」：`/`「对话」、`/dashboard`「概览」
- 分组「任务」：`/goals`「目标」、`/tasks`「任务」、`/inbox`「收件箱」、`/approvals`「审批」（角标：收件箱未读、待审批）
- 分组「知识」：`/memories`「记忆」、`/timeline`「时间线」。待确认记忆角标大于 0 时，「记忆」链到 `/memories?tab=review`
- 分组「系统」：`/settings`「设置」；底部通知铃。铃的下拉打开的同一轮焦点进入面板，不先停在铃上或页面空白。失败「重试」仍在这之后才拿焦点，Tab 和 Shift+Tab 留在面板里，Esc 回到铃；输入法还在组字或仍是处理键时按 Esc 不关闭。读取失败且还没有通知时写出原因并给出「重试」，不写成「暂无通知」。「全部已读」在写回来之前不会再发一次；成功且没有未读时，「全部已读」卸下的同一轮，焦点还在原处就落到第一条通知，不先停在页面空白
- 侧栏可收起（`localStorage.sidebar_collapsed`）；会话列表只在 chat 路由且展开时显示。会话标题是链到 `/chat/:id` 的链接，带与时间线相同的焦点环，当前会话标 `aria-current="page"`；删除仍是旁边的按钮。标题和摘要平时各占一行。键盘落到这一行或旁边的「删除对话」时写出整句。鼠标点上去仍是一行。这一钮平时只画垃圾桶，名字在悬停的 `title` 里。键盘落到时写出「删除对话」，图标让开。鼠标点上去仍是图标。列表还在加载时写「加载中…」。读取失败且还没有会话时写出原因并给出「重试」，不写成「暂无对话」；读取成功且为空时仍是这句。视口窄于 `md`（768px）时强制保持图标栏宽度，不改已保存的收起偏好，避免主内容被 240px 侧栏挤没。图标栏里的导航、「新对话」和通知铃平时只画图标，名字在悬停的 `title` 里。收起或展开平时也只画图标。键盘落到时写出这个名字，鼠标点上去仍是图标。有未读数时，导航的名字带上这个数

## Layout

[`frontend/src/Layout.tsx`](../../frontend/src/Layout.tsx) 渲染：

1. `<Sidebar>`（展开 240px / 收起或窄屏 68px）含会话列表 + 导航。
2. Banner 覆盖层：(a) 后端需认证但未配 token，(b) 后端不可用。
3. Toast 栈（右下，`bottom-20 right-4`）：WS 实时通知 + `useErrorStore` 错误 toast。出现时读出来：错误和警告打断当前朗读，通知、成功和普通提示等这一句说完。不把焦点抢过来。关掉这一钮平时只画叉。键盘落到时写出「关闭」，图标让开。鼠标点上去仍是叉。正文平时最多两行。键盘落到「关闭」或可以点开的那一块时写出整段。鼠标点上去仍是两行。点开实时通知的那一块带与时间线相同的焦点环。Enter 或空格打开详情；输入法还在组字或仍是处理键时这一下不打开，按住不连开，空格也不把页面滚走。关掉一条时，焦点还在这条上或落在页面空白处，同一轮落到下一条的同一个按钮（打开或关闭）；没有下一条就落到上一条；都没有就落到通知铃。已经把焦点移走时不再抢。从实时通知打开详情后，Esc 或关闭回到通知铃。到点自己消失时，焦点若还在这条上，也按同样的规则交出去。
4. `<main>` 含 `<Suspense>` + `<ErrorBoundary>` 包 `<Outlet />`。
5. 对话框：删除会话确认、`OnboardingWizard`、`NotificationDetailModal`、`QuickCaptureDialog`（桌面 `Alt+Shift+I` 或 Ctrl/Cmd+Shift+M。打开时焦点在输入框，这一框带与时间线相同的焦点环。对话框还开着时，再按 Ctrl/Cmd+Shift+M，或再收到一次桌面快捷捕获，不清掉已经写的字，也不打断这次保存。按键连发不另开。输入法还在组字或仍是处理键时这一下不打开。关掉之后再按才是一份新的。Esc 关闭并回到刚才的控件。输入框里按 Ctrl 或 Cmd + Enter 会保存；输入法还在组字或仍是处理键时这一下不保存，也不拦住。这次写回来之前不会再发一次。输入框和「保存」不禁用，「保存」标为忙碌，焦点留在上面。空内容时「保存」仍不可用。失败时这句话留着，焦点仍在刚才的输入框或按钮。提交过程中又改过的字留下；成功后若还和刚发出的一样，才显示已保存并关掉。这一句出现时读出来，等当前这一句说完。不把焦点抢过来。失败仍只走右下角提示。已经把焦点移走时不再抢。留下的草稿，焦点还在「保存」上或落在对话框面板、页面空白处时，同一轮落到输入框，不先停在页面空白。留下的空草稿若让「保存」变成不可用，也是这一轮，不先停在已经禁用的按钮上）。

Layout 在根处挂三个副作用 hook：`useNotifications()`（唯一持有 WS）、`useWsInvalidationBridge()`、会话/健康查询。仪表盘经 `LiveNotificationContext` 消费实时通知，不再自开第二条 socket。

## 构建 / 开发 / 测试

### Scripts（[`frontend/package.json`](../../frontend/package.json)）

| Script | 命令 |
|---|---|
| `dev` | `vite` |
| `build` | `tsc -b && vite build` |
| `preview` | `vite preview` |
| `test` | `vitest run` |
| `test:e2e` | `playwright test` |
| `test:e2e:real` | `playwright test --config=playwright.real.config.ts` |
| `lint` | `eslint src/ && prettier --check src/` |
| `format` | `prettier --write src/` |

### Vite 配置

[`frontend/vite.config.ts`](../../frontend/vite.config.ts)：

- 插件：`@vitejs/plugin-react`、`@tailwindcss/vite`。
- `envDir: rootDir` — 仓库根 `.env` 是 `VITE_API_HOST`/`VITE_API_PORT`/`VITE_AUTH_TOKEN` 源。
- Dev server 默认绑 `127.0.0.1:5173`；`VITE_DEV_LAN=1` 且 `VITE_AUTH_TOKEN` 非空时才 `host: true`。proxy `/api` 与 `/ws`（`ws: true`）。
- `define`：`__API_HOST__`、`__API_PORT__`（声明于 [`vite-env.d.ts`](../../frontend/src/vite-env.d.ts)）。
- 生产 `manualChunks`：分离 `vendor-markdown`、`vendor-react`、`vendor-icons`、通用 `vendor`、每页 chunk（`page-{name}`）。

### TypeScript

[`frontend/tsconfig.json`](../../frontend/tsconfig.json)：ES2020、bundler 解析、`jsx: react-jsx`、strict、`noEmit`、`forceConsistentCasingInFileNames`。`noUnusedLocals/Parameters` **关闭**。

### ESLint

[`frontend/eslint.config.js`](../../frontend/eslint.config.js)：Flat config，`@eslint/js` recommended + `typescript-eslint` recommended。忽略 `dist/`、`node_modules/`、`test-results/`。自定义：`no-unused-vars` warn（`_` 参量忽略）、`no-explicit-any` warn。

### Vitest

[`frontend/vitest.config.ts`](../../frontend/vitest.config.ts)：jsdom、包含 `src/**/*.test.{ts,tsx}`、setup 文件 [`src/test/setup.ts`](../../frontend/src/test/setup.ts)（注册 `@testing-library/jest-dom` + 每测 cleanup）。定义 `__API_HOST__`/`__API_PORT__`。

### Playwright

[`frontend/playwright.config.ts`](../../frontend/playwright.config.ts)：`testDir: "./e2e"`、60s 超时、headless、baseURL `http://localhost:5173`。`webServer.command: "npm run dev"`，复用已存在 server，120s 超时。

e2e 文件：[`e2e/chat-approval.spec.ts`](../../frontend/e2e/chat-approval.spec.ts)、[`e2e/extra-flows.spec.ts`](../../frontend/e2e/extra-flows.spec.ts)、[`e2e/trust-loops.spec.ts`](../../frontend/e2e/trust-loops.spec.ts)、[`e2e/real-backend.spec.ts`](../../frontend/e2e/real-backend.spec.ts)，配合 [`e2e/helpers.ts`](../../frontend/e2e/helpers.ts) 的 `MockApiRouter`。mock 套件覆盖导航、聊天发送、审批确认/拒绝流、`ask_user` 文本回答与取消、连续二次确认、首页发送、审批重载恢复、收件箱重试同步、proposed 确认进上下文、今日周期对比、仪表盘错误态、时间线、仪表盘数据主权面板；`real-backend.spec.ts` 走真实后端 + fake LLM。默认 `playwright.config.ts` 忽略 `real-backend.spec.ts`。

### 单元测试

- Vitest：`auth.test.ts`、`api/client.test.ts`，组件测试 `Button/Input/Dialog/Sidebar/MessageItem/ToolCallDisplay/ContextPanel/ConfirmationDialog/ChatView/AdoptionSummary/PeriodComparison/ui.snapshots`，页面测试 `Dashboard/Inbox/Memories/Goals/Tasks/Approvals/Timeline/Settings/Portrait/TrustReport/ChatPage`。strip/tool-label 工具也有测试。

## 工具

[`frontend/src/utils/`](../../frontend/src/utils/)：

- `stripToolMarkup.ts` — 去除助手文本中的内联工具调用标记（带测试）。
- `timeUtils.ts` — `timeAgo`、`isStagnant`（Goals 用）。
- `goalProgress.ts` — `goalProgressPercent`（Goals 进度：0–1 转为百分比，大于 1 视为已经是百分比）。
- `toolLabels.ts` / `toolLabels.test.ts` — 工具名友好中文标签。
- `notificationRoutes.ts` — 通知类型 → 路由 + 标签。
- `notificationUtils.ts` — `notificationPreview`（截断）。
